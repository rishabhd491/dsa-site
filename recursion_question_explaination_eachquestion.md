# TCS_NQT_Recursion_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Print Numbers from 1 to N

### What Exactly This Question Is Asking
- In simple words: Given `N`, print numbers from `1` to `N` using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N`, print numbers from `1` to `N` using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Print Numbers from 1 to N`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Print Numbers from N to 1

### What Exactly This Question Is Asking
- In simple words: Given `N`, print numbers from `N` to `1` using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N`, print numbers from `N` to `1` using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Print Numbers from N to 1`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Sum of First N Natural Numbers

### What Exactly This Question Is Asking
- In simple words: Find `1 + 2 + ...
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find `1 + 2 + ... + N` using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Sum of First N Natural Numbers`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Factorial of N

### What Exactly This Question Is Asking
- In simple words: Find `N!` using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find `N!` using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Factorial of N`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Fast Power (a^b)

### What Exactly This Question Is Asking
- In simple words: Compute `a^b` using recursion in `O(log b)` time.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Compute `a^b` using recursion in `O(log b)` time.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Fast Power (a^b)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Fibonacci Number (Memoized Recursion)

### What Exactly This Question Is Asking
- In simple words: Find the `n-th` Fibonacci number using recursion + memoization.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Find the `n-th` Fibonacci number using recursion + memoization.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Fibonacci Number (Memoized Recursion)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. GCD of Two Numbers (Euclid Recursion)

### What Exactly This Question Is Asking
- In simple words: Find GCD of `a` and `b` recursively.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Find GCD of `a` and `b` recursively.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q7. GCD of Two Numbers (Euclid Recursion)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Sum of Digits

### What Exactly This Question Is Asking
- In simple words: Given number `N`, return sum of digits using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given number `N`, return sum of digits using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Sum of Digits`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Reverse a Number

### What Exactly This Question Is Asking
- In simple words: Reverse an integer using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Reverse an integer using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Reverse a Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Check Palindrome String

### What Exactly This Question Is Asking
- In simple words: Check if a string is palindrome using recursion.
- Core solving focus: identify symmetry logic and compare mirrored positions.

### Problem Understanding (Detailed)
Check if a string is palindrome using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Check Palindrome String`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s = "abba"`
Walkthrough: Walkthrough: Compare first-last and inward pairs; all pairs match.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Count Zeros in a Number

### What Exactly This Question Is Asking
- In simple words: Count number of digit `0` in integer `N` recursively.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count number of digit `0` in integer `N` recursively.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Count Zeros in a Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Decimal to Binary (Recursive)

### What Exactly This Question Is Asking
- In simple words: Convert decimal number `N` to binary string using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Convert decimal number `N` to binary string using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Decimal to Binary (Recursive)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Check if Array is Sorted

### What Exactly This Question Is Asking
- In simple words: Check whether array is non-decreasing using recursion.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Check whether array is non-decreasing using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Check if Array is Sorted`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Linear Search Recursively

### What Exactly This Question Is Asking
- In simple words: Given array and key `x`, print `Yes` if present else `No`.
- Core solving focus: maintain valid search space and update low/high boundaries correctly.

### Problem Understanding (Detailed)
Given array and key `x`, print `Yes` if present else `No`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Linear Search Recursively`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [1,3,5,7,9], target = 7`
Walkthrough: Walkthrough: Mid comparison narrows search to target index.
Expected Output: index/value found indicator as required by the question.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. First Occurrence Index

### What Exactly This Question Is Asking
- In simple words: Find first index of `x` in array using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find first index of `x` in array using recursion. If absent print `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q15. First Occurrence Index`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Last Occurrence Index

### What Exactly This Question Is Asking
- In simple words: Find last index of `x` in array using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find last index of `x` in array using recursion. If absent print `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Last Occurrence Index`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Recursive Binary Search

### What Exactly This Question Is Asking
- In simple words: Find index of key `x` in sorted array using recursion.
- Core solving focus: maintain valid search space and update low/high boundaries correctly.

### Problem Understanding (Detailed)
Find index of key `x` in sorted array using recursion. If absent print `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Recursive Binary Search`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [1,3,5,7,9], target = 7`
Walkthrough: Walkthrough: Mid comparison narrows search to target index.
Expected Output: index/value found indicator as required by the question.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Reverse a String Recursively

### What Exactly This Question Is Asking
- In simple words: Reverse a string using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Reverse a string using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Reverse a String Recursively`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Remove Consecutive Duplicates in String

### What Exactly This Question Is Asking
- In simple words: Given string `s`, remove consecutive duplicate characters using recursion.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given string `s`, remove consecutive duplicate characters using recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Remove Consecutive Duplicates in String`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Move All 'x' to End

### What Exactly This Question Is Asking
- In simple words: Given string `s`, move all `'x'` to end while preserving order of other chars.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given string `s`, move all `'x'` to end while preserving order of other chars.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Move All 'x' to End`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Generate All Subsequences of a String

### What Exactly This Question Is Asking
- In simple words: Print all subsequences of string `s`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Print all subsequences of string `s`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Generate All Subsequences of a String`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Count Subsequences with Sum K

### What Exactly This Question Is Asking
- In simple words: Given array and `K`, count subsequences whose sum is exactly `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given array and `K`, count subsequences whose sum is exactly `K`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Count Subsequences with Sum K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Print One Subsequence with Sum K

### What Exactly This Question Is Asking
- In simple words: Print any one subsequence with sum `K`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Print any one subsequence with sum `K`. If none, print `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Print One Subsequence with Sum K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Combination Sum I

### What Exactly This Question Is Asking
- In simple words: Given distinct numbers and target `T`, find all combinations where numbers can be reused.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given distinct numbers and target `T`, find all combinations where numbers can be reused.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Combination Sum I`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Combination Sum II (Each Element Once, Duplicates in Input)

### What Exactly This Question Is Asking
- In simple words: Find unique combinations that sum to target.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find unique combinations that sum to target. Each index can be used once.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Combination Sum II (Each Element Once, Duplicates in Input)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Subset Sum List

### What Exactly This Question Is Asking
- In simple words: Given array, print all subset sums in sorted order.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Given array, print all subset sums in sorted order.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Subset Sum List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Permutations of an Array

### What Exactly This Question Is Asking
- In simple words: Print all permutations of given array.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Print all permutations of given array.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Permutations of an Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Unique Permutations of a String

### What Exactly This Question Is Asking
- In simple words: Print all unique permutations of a string that may contain duplicates.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Print all unique permutations of a string that may contain duplicates.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Unique Permutations of a String`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Generate Parentheses

### What Exactly This Question Is Asking
- In simple words: Given `n` pairs, generate all valid parentheses strings.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `n` pairs, generate all valid parentheses strings.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Generate Parentheses`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Letter Combinations of Phone Number

### What Exactly This Question Is Asking
- In simple words: Given digits `2-9`, print all possible letter combinations.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given digits `2-9`, print all possible letter combinations.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Letter Combinations of Phone Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Palindrome Partitioning

### What Exactly This Question Is Asking
- In simple words: Print all possible palindrome partitions of a string.
- Core solving focus: identify symmetry logic and compare mirrored positions.

### Problem Understanding (Detailed)
Print all possible palindrome partitions of a string.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Palindrome Partitioning`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s = "abba"`
Walkthrough: Walkthrough: Compare first-last and inward pairs; all pairs match.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. N-Queens (Count Solutions)

### What Exactly This Question Is Asking
- In simple words: Place `N` queens on `N x N` board so none attack each other.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Place `N` queens on `N x N` board so none attack each other. Print number of solutions.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q32. N-Queens (Count Solutions)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q33. Rat in a Maze (All Paths)

### What Exactly This Question Is Asking
- In simple words: Given `N x N` grid of `0/1` (`1` = open), print all paths from `(0,0)` to `(N-1,N-1)`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N x N` grid of `0/1` (`1` = open), print all paths from `(0,0)` to `(N-1,N-1)`.
Moves allowed: `D, L, R, U`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q33. Rat in a Maze (All Paths)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q34. Word Search in Grid

### What Exactly This Question Is Asking
- In simple words: Given board and word, check if word exists using DFS recursion.
- Core solving focus: maintain valid search space and update low/high boundaries correctly.

### Problem Understanding (Detailed)
Given board and word, check if word exists using DFS recursion.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q34. Word Search in Grid`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [1,3,5,7,9], target = 7`
Walkthrough: Walkthrough: Mid comparison narrows search to target index.
Expected Output: index/value found indicator as required by the question.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q35. Sudoku Solver

### What Exactly This Question Is Asking
- In simple words: Given a `9x9` Sudoku with `0` as empty cells, solve it.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given a `9x9` Sudoku with `0` as empty cells, solve it.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q35. Sudoku Solver`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q36. Tower of Hanoi

### What Exactly This Question Is Asking
- In simple words: Given `N` disks, print moves from rod A to C using B as auxiliary.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N` disks, print moves from rod A to C using B as auxiliary.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q36. Tower of Hanoi`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q37. Josephus Problem

### What Exactly This Question Is Asking
- In simple words: `N` people in circle, eliminate every `K-th` person.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
`N` people in circle, eliminate every `K-th` person. Print safe position (1-based).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q37. Josephus Problem`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q38. K-th Symbol in Grammar

### What Exactly This Question Is Asking
- In simple words: Row 1 is `0`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Row 1 is `0`. Next row: replace `0->01`, `1->10`.
Find `k-th` symbol in row `n` (1-indexed).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q38. K-th Symbol in Grammar`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q39. Sort a Stack Using Recursion

### What Exactly This Question Is Asking
- In simple words: Sort stack in ascending order using recursion only (no loops for sorting logic).
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Sort stack in ascending order using recursion only (no loops for sorting logic).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q39. Sort a Stack Using Recursion`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q40. Reverse a Stack Using Recursion

### What Exactly This Question Is Asking
- In simple words: Reverse stack using recursion only.
- Core solving focus: follow strict LIFO behavior and guard empty-stack cases.

### Problem Understanding (Detailed)
Reverse stack using recursion only.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q40. Reverse a Stack Using Recursion`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid recursion input (e.g., `n = 0/1`)
Walkthrough: Walkthrough: Base case should return immediately without further recursive calls.
Expected Output: base-case value/output.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
