# TCS_NQT_Bit_Manipulation_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Check i-th Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N` and index `i` (0-based), print whether i-th bit is set.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N` and index `i` (0-based), print whether i-th bit is set.

### Input Format Interpretation
- `N i`

### Output Format Interpretation
- `1` if set, else `0`

### Example 1 (Source-Style)
Input:
```text
13 2
```
Output:
```text
1
```

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Check i-th Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Set i-th Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N` and index `i`, set i-th bit and print updated number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N` and index `i`, set i-th bit and print updated number.

### Input Format Interpretation
- `N i`

### Output Format Interpretation
- Updated value

### Example 1 (Source-Style)
Input:
```text
9 1
```
Output:
```text
11
```

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Set i-th Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Clear i-th Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N` and index `i`, clear i-th bit and print updated number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N` and index `i`, clear i-th bit and print updated number.

### Input Format Interpretation
- `N i`

### Output Format Interpretation
- Updated value

### Example 1 (Source-Style)
Input:
```text
13 2
```
Output:
```text
9
```

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Clear i-th Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Toggle i-th Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N` and index `i`, toggle i-th bit.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N` and index `i`, toggle i-th bit.

### Input Format Interpretation
- `N i`

### Output Format Interpretation
- Updated value

### Example 1 (Source-Style)
Input:
```text
10 1
```
Output:
```text
8
```

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Toggle i-th Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Check Odd or Even Using Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, print `Odd` if odd else `Even`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, print `Odd` if odd else `Even`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Odd` / `Even`

### Example 1 (Source-Style)
Input:
```text
27
```
Output:
```text
Odd
```

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Check Odd or Even Using Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Check Power of 2

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, check whether it is power of two.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, check whether it is power of two.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
64
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Check Power of 2`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Check Power of 4

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, check whether it is power of four.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, check whether it is power of four.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
64
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Check Power of 4`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Count Set Bits (Simple)

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, count number of set bits in binary representation.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given integer `N`, count number of set bits in binary representation.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Count of set bits

### Example 1 (Source-Style)
Input:
```text
13
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Count Set Bits (Simple)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Count Set Bits (Brian Kernighan)

### What Exactly This Question Is Asking
- In simple words: Count set bits using `n = n & (n - 1)` optimization.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count set bits using `n = n & (n - 1)` optimization.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Count of set bits

### Example 1 (Source-Style)
Input:
```text
13
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Count Set Bits (Brian Kernighan)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Rightmost Set Bit Value

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, print value of rightmost set bit.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, print value of rightmost set bit.
If `N = 0`, print `0`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Value of rightmost set bit

### Example 1 (Source-Style)
Input:
```text
12
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Rightmost Set Bit Value`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Position of Rightmost Set Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, print 1-based index of rightmost set bit.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, print 1-based index of rightmost set bit.
If `N=0`, print `-1`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Position or `-1`

### Example 1 (Source-Style)
Input:
```text
18
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Position of Rightmost Set Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Clear Rightmost Set Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, clear its rightmost set bit and print result.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, clear its rightmost set bit and print result.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Updated number

### Example 1 (Source-Style)
Input:
```text
12
```
Output:
```text
8
```

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Clear Rightmost Set Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Set Rightmost Unset Bit

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, set its rightmost unset bit.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, set its rightmost unset bit.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Updated value

### Example 1 (Source-Style)
Input:
```text
10
```
Output:
```text
11
```

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Set Rightmost Unset Bit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Toggle Bits in Range [L, R]

### What Exactly This Question Is Asking
- In simple words: Given integer `N` and bit positions `L` and `R` (0-based, `L <= R`), toggle all bits in that range.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N` and bit positions `L` and `R` (0-based, `L <= R`), toggle all bits in that range.

### Input Format Interpretation
- `N L R`

### Output Format Interpretation
- Updated value

### Example 1 (Source-Style)
Input:
```text
17 1 3
```
Output:
```text
31
```

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Toggle Bits in Range [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. Swap Two Numbers Without Temp (XOR)

### What Exactly This Question Is Asking
- In simple words: Swap two integers without using third variable.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Swap two integers without using third variable.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- `B A`

### Example 1 (Source-Style)
Input:
```text
7 11
```
Output:
```text
11 7
```

Detailed Explanation:
- Start by identifying required operation from title: `Q15. Swap Two Numbers Without Temp (XOR)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Single Number (Others Appear Twice)

### What Exactly This Question Is Asking
- In simple words: Given array where all elements appear twice except one, find unique element.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array where all elements appear twice except one, find unique element.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- Unique number

### Example 1 (Source-Style)
Input:
```text
7
2 5 2 7 5 9 9
```
Output:
```text
7
```

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Single Number (Others Appear Twice)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Two Unique Numbers (Others Appear Twice)

### What Exactly This Question Is Asking
- In simple words: Given array where exactly two elements appear once and all others appear twice, print the two unique numbers in ascending order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array where exactly two elements appear once and all others appear twice, print the two unique numbers in ascending order.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- Two unique numbers sorted

### Example 1 (Source-Style)
Input:
```text
8
1 2 3 2 1 4 5 4
```
Output:
```text
3 5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Two Unique Numbers (Others Appear Twice)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Single Number (Others Appear Thrice)

### What Exactly This Question Is Asking
- In simple words: Given array where every element appears exactly 3 times except one, find that single element.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array where every element appears exactly 3 times except one, find that single element.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- Unique number

### Example 1 (Source-Style)
Input:
```text
7
2 2 3 2 5 5 5
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Single Number (Others Appear Thrice)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Missing Number from 0..N

### What Exactly This Question Is Asking
- In simple words: Given `N` and array of size `N` containing distinct numbers from `0..N` with one missing, find missing number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N` and array of size `N` containing distinct numbers from `0..N` with one missing, find missing number.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- Missing number

### Example 1 (Source-Style)
Input:
```text
5
0 1 2 4 5
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Missing Number from 0..N`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. XOR from 1 to N

### What Exactly This Question Is Asking
- In simple words: Given `N`, compute XOR of all numbers from `1` to `N`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N`, compute XOR of all numbers from `1` to `N`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- XOR value

### Example 1 (Source-Style)
Input:
```text
7
```
Output:
```text
0
```

Detailed Explanation:
- Start by identifying required operation from title: `Q20. XOR from 1 to N`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. XOR in Range [L, R]

### What Exactly This Question Is Asking
- In simple words: Given `L` and `R`, compute XOR of all numbers from `L` to `R`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `L` and `R`, compute XOR of all numbers from `L` to `R`.

### Input Format Interpretation
- `L R`

### Output Format Interpretation
- XOR value

### Example 1 (Source-Style)
Input:
```text
3 9
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q21. XOR in Range [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Bitwise AND of Range [L, R]

### What Exactly This Question Is Asking
- In simple words: Given `L` and `R`, compute bitwise AND of all numbers in range `[L, R]`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `L` and `R`, compute bitwise AND of all numbers in range `[L, R]`.

### Input Format Interpretation
- `L R`

### Output Format Interpretation
- AND value

### Example 1 (Source-Style)
Input:
```text
5 7
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Bitwise AND of Range [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Add Two Integers Without '+'

### What Exactly This Question Is Asking
- In simple words: Compute `A + B` using bit operations only.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Compute `A + B` using bit operations only.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- Sum

### Example 1 (Source-Style)
Input:
```text
12 15
```
Output:
```text
27
```

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Add Two Integers Without '+'`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Subtract Two Integers Without '-'

### What Exactly This Question Is Asking
- In simple words: Compute `A - B` using bit operations.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Compute `A - B` using bit operations.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- Difference

### Example 1 (Source-Style)
Input:
```text
20 7
```
Output:
```text
13
```

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Subtract Two Integers Without '-'`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Multiply Using Bit Shifts

### What Exactly This Question Is Asking
- In simple words: Compute `A * B` using shifts and addition.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Compute `A * B` using shifts and addition.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- Product

### Example 1 (Source-Style)
Input:
```text
13 9
```
Output:
```text
117
```

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Multiply Using Bit Shifts`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Integer Division Using Bits

### What Exactly This Question Is Asking
- In simple words: Compute integer division `A / B` without using `/`, `%`, `*`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Compute integer division `A / B` without using `/`, `%`, `*`.
Truncate toward zero.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- Quotient

### Example 1 (Source-Style)
Input:
```text
43 5
```
Output:
```text
8
```

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Integer Division Using Bits`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Count Total Set Bits from 1 to N (Optimized)

### What Exactly This Question Is Asking
- In simple words: Given `N`, find total set bits in binary representations of all numbers from `1` to `N`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given `N`, find total set bits in binary representations of all numbers from `1` to `N`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Total count

### Example 1 (Source-Style)
Input:
```text
5
```
Output:
```text
7
```

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Count Total Set Bits from 1 to N (Optimized)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Count Bits for Every Number 0..N

### What Exactly This Question Is Asking
- In simple words: For each integer `i` from `0` to `N`, output number of set bits in `i`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
For each integer `i` from `0` to `N`, output number of set bits in `i`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `N+1` integers

### Example 1 (Source-Style)
Input:
```text
5
```
Output:
```text
0 1 1 2 1 2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Count Bits for Every Number 0..N`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Most Significant Set Bit Position

### What Exactly This Question Is Asking
- In simple words: Given positive integer `N`, print 1-based position of most significant set bit.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given positive integer `N`, print 1-based position of most significant set bit.
If `N=0`, print `-1`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Position or `-1`

### Example 1 (Source-Style)
Input:
```text
18
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Most Significant Set Bit Position`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Smallest Power of 2 Greater Than or Equal to N

### What Exactly This Question Is Asking
- In simple words: Given integer `N > 0`, find smallest power of two `>= N`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N > 0`, find smallest power of two `>= N`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Answer power

### Example 1 (Source-Style)
Input:
```text
19
```
Output:
```text
32
```

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Smallest Power of 2 Greater Than or Equal to N`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Check Opposite Signs

### What Exactly This Question Is Asking
- In simple words: Given two integers `A` and `B`, determine if they have opposite signs.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given two integers `A` and `B`, determine if they have opposite signs.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- `Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
-7 11
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Check Opposite Signs`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. Hamming Distance Between Two Integers

### What Exactly This Question Is Asking
- In simple words: Given integers `A` and `B`, find number of differing bit positions.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integers `A` and `B`, find number of differing bit positions.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- Hamming distance

### Example 1 (Source-Style)
Input:
```text
10 14
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q32. Hamming Distance Between Two Integers`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q33. Total Hamming Distance in Array

### What Exactly This Question Is Asking
- In simple words: Given array of `N` non-negative integers, compute total Hamming distance among all unordered pairs.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array of `N` non-negative integers, compute total Hamming distance among all unordered pairs.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- Total Hamming distance

### Example 1 (Source-Style)
Input:
```text
3
4 14 2
```
Output:
```text
6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q33. Total Hamming Distance in Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q34. Generate All Subsets (Bitmask)

### What Exactly This Question Is Asking
- In simple words: Given `N` elements, print all subsets using bitmasking.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Given `N` elements, print all subsets using bitmasking.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- One subset per line

### Example 1 (Source-Style)
Input:
```text
3
1 2 3
```
Output (one valid order):
```text

1
2
1 2
3
1 3
2 3
1 2 3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q34. Generate All Subsets (Bitmask)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q35. Subset Sum Existence (Bitmask, Small N)

### What Exactly This Question Is Asking
- In simple words: Given array of size `N <= 20` and target `K`, check if any subset sum equals `K`.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Given array of size `N <= 20` and target `K`, check if any subset sum equals `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
- `Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
4 11
2 3 7 8
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q35. Subset Sum Existence (Bitmask, Small N)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q36. Gray Code Generation (n bits)

### What Exactly This Question Is Asking
- In simple words: Generate Gray code sequence for given `n` bits.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Generate Gray code sequence for given `n` bits.

### Input Format Interpretation
- One integer `n`

### Output Format Interpretation
- `2^n` Gray code integers

### Example 1 (Source-Style)
Input:
```text
2
```
Output:
```text
0 1 3 2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q36. Gray Code Generation (n bits)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q37. Check if Binary String Has Alternate Bits

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, check whether its binary representation has alternating bits.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, check whether its binary representation has alternating bits.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
10
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q37. Check if Binary String Has Alternate Bits`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q38. Find Missing and Repeating Number (XOR Method)

### What Exactly This Question Is Asking
- In simple words: Given array of size `N` containing numbers `1..N` with one missing and one repeating, find both.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array of size `N` containing numbers `1..N` with one missing and one repeating, find both.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- `missing repeating`

### Example 1 (Source-Style)
Input:
```text
5
1 2 2 4 5
```
Output:
```text
3 2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q38. Find Missing and Repeating Number (XOR Method)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q39. Unique Lowercase Characters in String (Bitmask)

### What Exactly This Question Is Asking
- In simple words: Given lowercase string, check if all characters are unique using bitmask.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given lowercase string, check if all characters are unique using bitmask.

### Input Format Interpretation
- One lowercase string

### Output Format Interpretation
- `Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
coding
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q39. Unique Lowercase Characters in String (Bitmask)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q40. Story-Based: Access Permission Bitmask

### What Exactly This Question Is Asking
- In simple words: A system encodes permissions in bits: - bit 0: READ - bit 1: WRITE - bit 2: EXECUTE Given initial mask and `Q` operations: - `GRANT x` - `REVOKE x` - `CHECK x` Print `Yes/No` for each CHECK.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A system encodes permissions in bits:
- bit 0: READ
- bit 1: WRITE
- bit 2: EXECUTE

Given initial mask and `Q` operations:
- `GRANT x`
- `REVOKE x`
- `CHECK x`
Print `Yes/No` for each CHECK.

### Input Format Interpretation
- First line: `mask Q`
- Next `Q` lines: operation and bit index `x`

### Output Format Interpretation
- For each `CHECK`, print `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
1 5
CHECK 0
CHECK 1
GRANT 1
CHECK 1
REVOKE 0
```
Output:
```text
Yes
No
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q40. Story-Based: Access Permission Bitmask`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q41. Story-Based: Minimum Button Presses (XOR Distance)

### What Exactly This Question Is Asking
- In simple words: Two binary lock states are represented as integers `A` and `B`.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Two binary lock states are represented as integers `A` and `B`.
One button press flips exactly one bit. Find minimum button presses to reach `B` from `A`.

### Input Format Interpretation
- `A B`

### Output Format Interpretation
- Minimum presses

### Example 1 (Source-Style)
Input:
```text
10 14
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q41. Story-Based: Minimum Button Presses (XOR Distance)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q42. Maximum XOR Pair (O(n^2) for NQT constraints)

### What Exactly This Question Is Asking
- In simple words: Given array, find maximum XOR value among all pairs.
- Core solving focus: track complements or pointers and avoid invalid duplicates.

### Problem Understanding (Detailed)
Given array, find maximum XOR value among all pairs.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- Maximum XOR value

### Example 1 (Source-Style)
Input:
```text
6
3 10 5 25 2 8
```
Output:
```text
28
```

Detailed Explanation:
- Start by identifying required operation from title: `Q42. Maximum XOR Pair (O(n^2) for NQT constraints)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q43. Count Subarrays with XOR = K

### What Exactly This Question Is Asking
- In simple words: Given array and `K`, count number of subarrays with XOR exactly `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given array and `K`, count number of subarrays with XOR exactly `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
- Count

### Example 1 (Source-Style)
Input:
```text
5 6
4 2 2 6 4
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q43. Count Subarrays with XOR = K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q44. Longest Subarray with XOR = K

### What Exactly This Question Is Asking
- In simple words: Given array and `K`, find length of longest subarray with XOR equal to `K`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given array and `K`, find length of longest subarray with XOR equal to `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
- Maximum length

### Example 1 (Source-Style)
Input:
```text
6 5
5 6 7 8 9 10
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q44. Longest Subarray with XOR = K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q45. Count Numbers in Array Having Exactly K Set Bits

### What Exactly This Question Is Asking
- In simple words: Given array and integer `K`, count elements whose binary representation has exactly `K` set bits.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given array and integer `K`, count elements whose binary representation has exactly `K` set bits.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` non-negative integers

### Output Format Interpretation
- Count

### Example 1 (Source-Style)
Input:
```text
6 2
3 5 7 8 9 10
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q45. Count Numbers in Array Having Exactly K Set Bits`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
