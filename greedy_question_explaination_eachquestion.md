# TCS_NQT_Greedy_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Assign Cookies (Maximum Content Children)

### What Exactly This Question Is Asking
- In simple words: Given children greed factors and cookie sizes, each child gets at most one cookie.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given children greed factors and cookie sizes, each child gets at most one cookie.
Maximize number of satisfied children.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Assign Cookies (Maximum Content Children)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Lemonade Change

### What Exactly This Question Is Asking
- In simple words: Customers pay using 5, 10, or 20.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Customers pay using 5, 10, or 20. Each lemonade costs 5.
Return `Yes` if change can be given to all in order, else `No`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Lemonade Change`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Minimum Coins (Canonical Denominations)

### What Exactly This Question Is Asking
- In simple words: Given amount `A`, find minimum number of notes using denominations: `[2000, 500, 200, 100, 50, 20, 10, 5, 2, 1]`.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given amount `A`, find minimum number of notes using denominations:
`[2000, 500, 200, 100, 50, 20, 10, 5, 2, 1]`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Minimum Coins (Canonical Denominations)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Fractional Knapsack

### What Exactly This Question Is Asking
- In simple words: Given `N` items with value and weight, and bag capacity `W`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N` items with value and weight, and bag capacity `W`.
You can take fractions of items. Maximize value.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Fractional Knapsack`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Activity Selection

### What Exactly This Question Is Asking
- In simple words: Given activities with start and finish times, find maximum number of non-overlapping activities.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given activities with start and finish times, find maximum number of non-overlapping activities.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Activity Selection`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. N Meetings in One Room (Count + Order)

### What Exactly This Question Is Asking
- In simple words: Given start and end arrays, schedule maximum meetings in one room.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given start and end arrays, schedule maximum meetings in one room.
Print count and selected meeting indices (1-based).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q6. N Meetings in One Room (Count + Order)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Minimum Platforms (Railway)

### What Exactly This Question Is Asking
- In simple words: Given arrival and departure times of trains, find minimum platforms required so no train waits.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given arrival and departure times of trains, find minimum platforms required so no train waits.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Minimum Platforms (Railway)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Meeting Rooms II (Minimum Rooms)

### What Exactly This Question Is Asking
- In simple words: Given meeting intervals, return minimum number of rooms required.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given meeting intervals, return minimum number of rooms required.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Meeting Rooms II (Minimum Rooms)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Erase Overlap Intervals

### What Exactly This Question Is Asking
- In simple words: Given intervals, remove minimum number so remaining intervals are non-overlapping.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given intervals, remove minimum number so remaining intervals are non-overlapping.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Erase Overlap Intervals`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Minimum Arrows to Burst Balloons

### What Exactly This Question Is Asking
- In simple words: Each balloon is interval `[xstart, xend]`.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Each balloon is interval `[xstart, xend]`. One arrow at `x` bursts all containing `x`.
Return minimum arrows.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Minimum Arrows to Burst Balloons`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Merge Overlapping Intervals

### What Exactly This Question Is Asking
- In simple words: Merge all overlapping intervals and print merged list.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Merge all overlapping intervals and print merged list.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Merge Overlapping Intervals`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Insert Interval

### What Exactly This Question Is Asking
- In simple words: Given sorted non-overlapping intervals and a new interval, insert and merge if needed.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given sorted non-overlapping intervals and a new interval, insert and merge if needed.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Insert Interval`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Job Sequencing with Deadlines

### What Exactly This Question Is Asking
- In simple words: Each job has deadline and profit, takes 1 unit time.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Each job has deadline and profit, takes 1 unit time.
Schedule jobs to maximize total profit.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Job Sequencing with Deadlines`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Minimum Cost to Connect Ropes

### What Exactly This Question Is Asking
- In simple words: Connect all ropes into one rope.
- Core solving focus: define clear state transitions and base cases before iteration/recursion.

### Problem Understanding (Detailed)
Connect all ropes into one rope. Cost of connecting two ropes is their sum.
Find minimum total cost.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Minimum Cost to Connect Ropes`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. Gas Station Circular Tour

### What Exactly This Question Is Asking
- In simple words: Given gas and cost arrays, find starting index to complete circle, else `-1`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given gas and cost arrays, find starting index to complete circle, else `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q15. Gas Station Circular Tour`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Jump Game I (Reach Last Index)

### What Exactly This Question Is Asking
- In simple words: Given max jump lengths, check if last index is reachable.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given max jump lengths, check if last index is reachable.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Jump Game I (Reach Last Index)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Jump Game II (Minimum Jumps)

### What Exactly This Question Is Asking
- In simple words: Find minimum jumps to reach last index.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Find minimum jumps to reach last index. If impossible, print `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Jump Game II (Minimum Jumps)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Candy Distribution

### What Exactly This Question Is Asking
- In simple words: Each child has rating.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Each child has rating. Give at least 1 candy each.
Higher rating than neighbor must get more candies.
Find minimum total candies.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Candy Distribution`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Best Time to Buy and Sell Stock II

### What Exactly This Question Is Asking
- In simple words: Unlimited transactions allowed (cannot hold multiple stocks).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Unlimited transactions allowed (cannot hold multiple stocks).
Find maximum profit.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Best Time to Buy and Sell Stock II`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Stock Buy-Sell with Transaction Fee

### What Exactly This Question Is Asking
- In simple words: Each sell transaction costs fee `F`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Each sell transaction costs fee `F`. Maximize profit.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Stock Buy-Sell with Transaction Fee`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Can Place Flowers

### What Exactly This Question Is Asking
- In simple words: Given flowerbed (`0` empty, `1` occupied) and `k`, can you place `k` new flowers without adjacent flowers?
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given flowerbed (`0` empty, `1` occupied) and `k`, can you place `k` new flowers
without adjacent flowers?

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Can Place Flowers`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Partition Labels

### What Exactly This Question Is Asking
- In simple words: Split string into maximum partitions such that each character appears in at most one partition.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Split string into maximum partitions such that each character appears in at most one partition.
Print partition lengths.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Partition Labels`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Remove K Digits (Smallest Number)

### What Exactly This Question Is Asking
- In simple words: Given numeric string and `k`, remove exactly `k` digits to form smallest possible number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given numeric string and `k`, remove exactly `k` digits to form smallest possible number.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Remove K Digits (Smallest Number)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Valid Parenthesis String with `*`

### What Exactly This Question Is Asking
- In simple words: String contains `(`, `)`, `*` where `*` can be `(` or `)` or empty.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
String contains `(`, `)`, `*` where `*` can be `(` or `)` or empty.
Check if string can be valid.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Valid Parenthesis String with `*``.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Reorganize String (No Adjacent Equal)

### What Exactly This Question Is Asking
- In simple words: Rearrange characters so no two adjacent are same.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Rearrange characters so no two adjacent are same.
If impossible print `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Reorganize String (No Adjacent Equal)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Task Scheduler with Cooldown

### What Exactly This Question Is Asking
- In simple words: Given task letters and cooldown `n`, find minimum intervals needed.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given task letters and cooldown `n`, find minimum intervals needed.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Task Scheduler with Cooldown`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Boats to Save People

### What Exactly This Question Is Asking
- In simple words: Each boat carries at most 2 people with total weight <= limit.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Each boat carries at most 2 people with total weight <= limit.
Find minimum boats.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Boats to Save People`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Mice and Holes (Minimum Time)

### What Exactly This Question Is Asking
- In simple words: Each mouse goes to one hole.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Each mouse goes to one hole. Time is absolute distance.
Minimize the maximum time taken by any mouse.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Mice and Holes (Minimum Time)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Two City Scheduling

### What Exactly This Question Is Asking
- In simple words: `2N` people, each has cost to city A and B.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
`2N` people, each has cost to city A and B.
Send exactly `N` to each city with minimum total cost.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Two City Scheduling`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Course Schedule III

### What Exactly This Question Is Asking
- In simple words: Each course has duration and last day.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Each course has duration and last day.
Take max number of courses.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Course Schedule III`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Minimum Refuel Stops

### What Exactly This Question Is Asking
- In simple words: Target distance `T`, start fuel `F`, stations `(position, fuel)`.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Target distance `T`, start fuel `F`, stations `(position, fuel)`.
Find minimum stops to reach target, else `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Minimum Refuel Stops`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. IPO (Maximize Capital)

### What Exactly This Question Is Asking
- In simple words: You can do at most `k` projects.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
You can do at most `k` projects.
Project `i` needs capital `c[i]` and yields profit `p[i]`.
Starting capital is `w`. Maximize final capital.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q32. IPO (Maximize Capital)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q33. Queue Reconstruction by Height

### What Exactly This Question Is Asking
- In simple words: Each person is `(height, k)` where `k` is number of people with height >= `height` standing before them.
- Core solving focus: follow FIFO order and handle front/rear transitions correctly.

### Problem Understanding (Detailed)
Each person is `(height, k)` where `k` is number of people with height >= `height`
standing before them. Reconstruct queue.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q33. Queue Reconstruction by Height`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q34. Largest Number from Array

### What Exactly This Question Is Asking
- In simple words: Arrange non-negative integers to form the largest possible number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Arrange non-negative integers to form the largest possible number.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q34. Largest Number from Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q35. Minimize Maximum Difference Between Heights

### What Exactly This Question Is Asking
- In simple words: Given heights and integer `K`, each height can be increased or decreased by `K` once.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given heights and integer `K`, each height can be increased or decreased by `K` once.
Find minimum possible difference between highest and lowest.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q35. Minimize Maximum Difference Between Heights`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q36. Minimum Patches to Cover Range [1, N]

### What Exactly This Question Is Asking
- In simple words: Given sorted positive array `nums`, add minimum numbers (patches) so every number in `[1, N]` can be formed as subset sum.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given sorted positive array `nums`, add minimum numbers (patches) so every number
in `[1, N]` can be formed as subset sum.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q36. Minimum Patches to Cover Range [1, N]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal instance where choices are obvious
Walkthrough: Walkthrough: Verify greedy local choice and confirm final result is globally optimal.
Expected Output: optimal count/cost/profit as requested.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
