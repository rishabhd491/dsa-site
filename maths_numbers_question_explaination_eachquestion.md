# TCS_NQT_Maths_Numbers_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Count Digits of a Number

### What Exactly This Question Is Asking
- In simple words: Given an integer `N`, count how many digits it has.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given an integer `N`, count how many digits it has.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- One integer: number of digits

### Constraint Notes
- `-10^18 <= N <= 10^18`

### Example 1 (Source-Style)
Input:
```text
10590
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Count Digits of a Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Sum of Digits

### What Exactly This Question Is Asking
- In simple words: Given an integer `N`, find sum of its digits.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an integer `N`, find sum of its digits.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Sum of digits

### Example 1 (Source-Style)
Input:
```text
48291
```
Output:
```text
24
```

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Sum of Digits`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Reverse a Number

### What Exactly This Question Is Asking
- In simple words: Given an integer `N`, reverse its digits.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an integer `N`, reverse its digits.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Reversed number

### Example 1 (Source-Style)
Input:
```text
12340
```
Output:
```text
4321
```

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Reverse a Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Check Palindrome Number

### What Exactly This Question Is Asking
- In simple words: Given an integer `N`, check whether it is palindrome.
- Core solving focus: identify symmetry logic and compare mirrored positions.

### Problem Understanding (Detailed)
Given an integer `N`, check whether it is palindrome.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Print `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
12321
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Check Palindrome Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s = "abba"`
Walkthrough: Walkthrough: Compare first-last and inward pairs; all pairs match.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Maximum and Minimum Digit in a Number

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, find maximum and minimum digit.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given integer `N`, find maximum and minimum digit.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `minDigit maxDigit`

### Example 1 (Source-Style)
Input:
```text
503908
```
Output:
```text
0 9
```

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Maximum and Minimum Digit in a Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Check Armstrong Number

### What Exactly This Question Is Asking
- In simple words: A number is Armstrong if sum of each digit raised to power `number_of_digits` equals the number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A number is Armstrong if sum of each digit raised to power `number_of_digits` equals the number.
Check if `N` is Armstrong.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
153
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Check Armstrong Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Check Perfect Number

### What Exactly This Question Is Asking
- In simple words: A number is perfect if sum of proper divisors equals the number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A number is perfect if sum of proper divisors equals the number.
Check if `N` is perfect.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
28
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Check Perfect Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Check Strong Number

### What Exactly This Question Is Asking
- In simple words: A number is strong if sum of factorial of its digits equals the number.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A number is strong if sum of factorial of its digits equals the number.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
145
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Check Strong Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Check Prime Number

### What Exactly This Question Is Asking
- In simple words: Check whether integer `N` is prime.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Check whether integer `N` is prime.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` if prime, else `No`

### Example 1 (Source-Style)
Input:
```text
29
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Check Prime Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Print All Primes in Range [L, R]

### What Exactly This Question Is Asking
- In simple words: Given `L` and `R`, print all prime numbers in that inclusive range.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `L` and `R`, print all prime numbers in that inclusive range.
If none, print `-1`.

### Input Format Interpretation
- Two integers `L R`

### Output Format Interpretation
- Primes separated by space or `-1`

### Constraint Notes
- `1 <= L <= R <= 10^7`

### Example 1 (Source-Style)
Input:
```text
10 30
```
Output:
```text
11 13 17 19 23 29
```

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Print All Primes in Range [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Prime Factorization

### What Exactly This Question Is Asking
- In simple words: Print prime factors of `N` in non-decreasing order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Print prime factors of `N` in non-decreasing order.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Prime factors separated by space

### Example 1 (Source-Style)
Input:
```text
84
```
Output:
```text
2 2 3 7
```

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Prime Factorization`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Count Number of Divisors

### What Exactly This Question Is Asking
- In simple words: Given `N`, count how many positive divisors it has.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given `N`, count how many positive divisors it has.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Number of divisors

### Example 1 (Source-Style)
Input:
```text
36
```
Output:
```text
9
```

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Count Number of Divisors`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Sum of Divisors

### What Exactly This Question Is Asking
- In simple words: Given `N`, compute sum of all positive divisors.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N`, compute sum of all positive divisors.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Sum of divisors

### Example 1 (Source-Style)
Input:
```text
12
```
Output:
```text
28
```

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Sum of Divisors`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Check if Number is Sum of Two Primes

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, determine whether it can be represented as sum of two prime numbers.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, determine whether it can be represented as sum of two prime numbers.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
34
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Check if Number is Sum of Two Primes`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. GCD and LCM of Two Numbers

### What Exactly This Question Is Asking
- In simple words: Given `A` and `B`, find GCD and LCM.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `A` and `B`, find GCD and LCM.

### Input Format Interpretation
- Two integers `A B`

### Output Format Interpretation
- `gcd lcm`

### Example 1 (Source-Style)
Input:
```text
12 18
```
Output:
```text
6 36
```

Detailed Explanation:
- Start by identifying required operation from title: `Q15. GCD and LCM of Two Numbers`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. GCD of an Array

### What Exactly This Question Is Asking
- In simple words: Given array of `N` integers, find gcd of all elements.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array of `N` integers, find gcd of all elements.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- GCD of all numbers

### Example 1 (Source-Style)
Input:
```text
4
24 36 60 84
```
Output:
```text
12
```

Detailed Explanation:
- Start by identifying required operation from title: `Q16. GCD of an Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. LCM of an Array

### What Exactly This Question Is Asking
- In simple words: Given array of `N` positive integers, compute LCM of all.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array of `N` positive integers, compute LCM of all.
If overflow beyond signed 64-bit, print `-1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- LCM or `-1`

### Example 1 (Source-Style)
Input:
```text
3
4 6 8
```
Output:
```text
24
```

Detailed Explanation:
- Start by identifying required operation from title: `Q17. LCM of an Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Modular Exponentiation (a^b mod m)

### What Exactly This Question Is Asking
- In simple words: Given `a`, `b`, `m`, compute `(a^b) % m` efficiently.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `a`, `b`, `m`, compute `(a^b) % m` efficiently.

### Input Format Interpretation
- Three integers `a b m`

### Output Format Interpretation
- One integer

### Example 1 (Source-Style)
Input:
```text
2 10 1000
```
Output:
```text
24
```

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Modular Exponentiation (a^b mod m)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Decimal to Binary Conversion

### What Exactly This Question Is Asking
- In simple words: Convert decimal integer `N` to binary representation.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Convert decimal integer `N` to binary representation.

### Input Format Interpretation
- One non-negative integer `N`

### Output Format Interpretation
- Binary string

### Example 1 (Source-Style)
Input:
```text
13
```
Output:
```text
1101
```

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Decimal to Binary Conversion`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Binary to Decimal Conversion

### What Exactly This Question Is Asking
- In simple words: Given a binary string, convert it to decimal.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given a binary string, convert it to decimal.

### Input Format Interpretation
- Binary string `S`

### Output Format Interpretation
- Decimal value

### Example 1 (Source-Style)
Input:
```text
101101
```
Output:
```text
45
```

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Binary to Decimal Conversion`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Check Power of Two

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, check whether it is a power of 2.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, check whether it is a power of 2.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
64
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Check Power of Two`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. AP: Nth Term and Sum of First N Terms

### What Exactly This Question Is Asking
- In simple words: Given first term `a`, common difference `d`, and `n`, find: 1.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given first term `a`, common difference `d`, and `n`, find:
1. nth term
2. sum of first n terms

### Input Format Interpretation
- `a d n`

### Output Format Interpretation
- `nthTerm sum`

### Example 1 (Source-Style)
Input:
```text
2 3 5
```
Output:
```text
14 40
```

Detailed Explanation:
- Start by identifying required operation from title: `Q22. AP: Nth Term and Sum of First N Terms`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. GP: Nth Term and Sum of First N Terms

### What Exactly This Question Is Asking
- In simple words: Given first term `a`, common ratio `r`, and `n`, find nth term and sum of first n terms.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given first term `a`, common ratio `r`, and `n`, find nth term and sum of first n terms.

### Input Format Interpretation
- `a r n`

### Output Format Interpretation
- `nthTerm sum` (floating precision)

### Example 1 (Source-Style)
Input:
```text
3 2 4
```
Output:
```text
24 45
```

Detailed Explanation:
- Start by identifying required operation from title: `Q23. GP: Nth Term and Sum of First N Terms`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Fibonacci Nth Term (Iterative)

### What Exactly This Question Is Asking
- In simple words: Given `N`, find nth Fibonacci (0-indexed): `F0=0`, `F1=1`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N`, find nth Fibonacci (0-indexed): `F0=0`, `F1=1`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `F(N)`

### Example 1 (Source-Style)
Input:
```text
10
```
Output:
```text
55
```

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Fibonacci Nth Term (Iterative)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Fibonacci Nth Term Mod 1e9+7 (Fast Doubling)

### What Exactly This Question Is Asking
- In simple words: Given very large `N` (`<= 10^18`), compute `F(N) mod 1e9+7`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given very large `N` (`<= 10^18`), compute `F(N) mod 1e9+7`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `F(N) mod 1000000007`

### Example 1 (Source-Style)
Input:
```text
50
```
Output:
```text
586268941
```

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Fibonacci Nth Term Mod 1e9+7 (Fast Doubling)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Factorial of Large Number

### What Exactly This Question Is Asking
- In simple words: Given `N` (`0 <= N <= 5000`), print `N!` exactly.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N` (`0 <= N <= 5000`), print `N!` exactly.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Exact value of `N!`

### Example 1 (Source-Style)
Input:
```text
10
```
Output:
```text
3628800
```

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Factorial of Large Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Trailing Zeros in Factorial

### What Exactly This Question Is Asking
- In simple words: Given `N`, compute number of trailing zeros in `N!`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N`, compute number of trailing zeros in `N!`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Number of trailing zeros

### Example 1 (Source-Style)
Input:
```text
100
```
Output:
```text
24
```

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Trailing Zeros in Factorial`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Compute nCr mod 1e9+7

### What Exactly This Question Is Asking
- In simple words: Given `n` and `r`, compute `nCr mod 1e9+7`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `n` and `r`, compute `nCr mod 1e9+7`.

### Input Format Interpretation
- Two integers `n r`

### Output Format Interpretation
- `nCr mod 1e9+7`

### Constraint Notes
- `0 <= r <= n <= 10^6`

### Example 1 (Source-Style)
Input:
```text
5 2
```
Output:
```text
10
```

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Compute nCr mod 1e9+7`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Story-Based: Count Product Codes with No Repeated Digits in [L, R]

### What Exactly This Question Is Asking
- In simple words: Given product code range `[L, R]`, count how many numbers have no repeated digit.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given product code range `[L, R]`, count how many numbers have no repeated digit.

### Input Format Interpretation
- Two integers `L R`

### Output Format Interpretation
- Count

### Example 1 (Source-Style)
Input:
```text
10 25
```
Output:
```text
14
```

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Story-Based: Count Product Codes with No Repeated Digits in [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Floor Square Root

### What Exactly This Question Is Asking
- In simple words: Given non-negative integer `N`, find floor of `sqrt(N)`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given non-negative integer `N`, find floor of `sqrt(N)`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `floor(sqrt(N))`

### Example 1 (Source-Style)
Input:
```text
27
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Floor Square Root`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Count Numbers Divisible by A or B in [L, R]

### What Exactly This Question Is Asking
- In simple words: Given `L, R, A, B`, count numbers in range `[L, R]` divisible by `A` or `B`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given `L, R, A, B`, count numbers in range `[L, R]` divisible by `A` or `B`.

### Input Format Interpretation
- `L R A B`

### Output Format Interpretation
- Count

### Example 1 (Source-Style)
Input:
```text
1 20 3 5
```
Output:
```text
9
```

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Count Numbers Divisible by A or B in [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. Digital Root (Single Digit Sum)

### What Exactly This Question Is Asking
- In simple words: Repeatedly sum digits of `N` until one digit remains.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Repeatedly sum digits of `N` until one digit remains.

### Input Format Interpretation
- One non-negative integer `N`

### Output Format Interpretation
- Digital root

### Example 1 (Source-Style)
Input:
```text
9875
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q32. Digital Root (Single Digit Sum)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q33. Check Leap Year

### What Exactly This Question Is Asking
- In simple words: Given year `Y`, check whether it is leap year.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given year `Y`, check whether it is leap year.

### Input Format Interpretation
- One integer `Y`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
2024
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q33. Check Leap Year`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q34. Days in a Month

### What Exactly This Question Is Asking
- In simple words: Given month `M` (1-12) and year `Y`, print number of days in that month.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given month `M` (1-12) and year `Y`, print number of days in that month.

### Input Format Interpretation
- `M Y`

### Output Format Interpretation
- Number of days

### Example 1 (Source-Style)
Input:
```text
2 2024
```
Output:
```text
29
```

Detailed Explanation:
- Start by identifying required operation from title: `Q34. Days in a Month`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q35. Euler Totient Function phi(N)

### What Exactly This Question Is Asking
- In simple words: Given integer `N`, compute Euler's Totient `phi(N)` = count of numbers in `[1..N]` coprime with `N`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given integer `N`, compute Euler's Totient `phi(N)` = count of numbers in `[1..N]` coprime with `N`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `phi(N)`

### Example 1 (Source-Style)
Input:
```text
9
```
Output:
```text
6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q35. Euler Totient Function phi(N)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q36. Number of Digits in N! (Without Computing N!)

### What Exactly This Question Is Asking
- In simple words: Given `N`, find number of digits in `N!` efficiently.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `N`, find number of digits in `N!` efficiently.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- Number of digits in `N!`

### Constraint Notes
- `0 <= N <= 10^7`

### Example 1 (Source-Style)
Input:
```text
5
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q36. Number of Digits in N! (Without Computing N!)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q37. Check Happy Number

### What Exactly This Question Is Asking
- In simple words: A happy number reaches `1` when repeatedly replaced by sum of squares of digits.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A happy number reaches `1` when repeatedly replaced by sum of squares of digits.
Given `N`, check if happy.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
19
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q37. Check Happy Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q38. Check Automorphic Number

### What Exactly This Question Is Asking
- In simple words: A number `N` is automorphic if square of `N` ends with digits of `N`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A number `N` is automorphic if square of `N` ends with digits of `N`.

### Input Format Interpretation
- One integer `N`

### Output Format Interpretation
- `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
76
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q38. Check Automorphic Number`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q39. Story-Based: Minimum Time Slot that Fits Cyclic Schedule (LCM use)

### What Exactly This Question Is Asking
- In simple words: Two machines repeat tasks every `A` and `B` minutes.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Two machines repeat tasks every `A` and `B` minutes.
Find first time (in minutes) when both start together again.

### Input Format Interpretation
- Two integers `A B`

### Output Format Interpretation
- First common time

### Example 1 (Source-Style)
Input:
```text
12 18
```
Output:
```text
36
```

Detailed Explanation:
- Start by identifying required operation from title: `Q39. Story-Based: Minimum Time Slot that Fits Cyclic Schedule (LCM use)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q40. Story-Based: Odd-Even Vehicle Rule

### What Exactly This Question Is Asking
- In simple words: Given date `D` and last digits of `N` vehicle numbers, count allowed vehicles: - if `D` is even, allow even last digits - if `D` is odd, allow odd last digits
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given date `D` and last digits of `N` vehicle numbers, count allowed vehicles:
- if `D` is even, allow even last digits
- if `D` is odd, allow odd last digits

### Input Format Interpretation
- First line: `N D`
- Second line: `N` digits (0-9)

### Output Format Interpretation
- Count of allowed vehicles

### Example 1 (Source-Style)
Input:
```text
6 13
2 5 8 1 7 4
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q40. Story-Based: Odd-Even Vehicle Rule`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: minimal valid input as per constraints
Walkthrough: Walkthrough: Execute the same steps as normal case on boundary values.
Expected Output: correct boundary behavior.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
