# TCS_NQT_Story_Simulation_Master_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Odd-Even Vehicle Rule

### What Exactly This Question Is Asking
- In simple words: City rule: - On even date `D`, vehicles with even last digit are allowed.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
City rule:
- On even date `D`, vehicles with even last digit are allowed.
- On odd date `D`, vehicles with odd last digit are allowed.
Given `N` and `N` last digits, count allowed vehicles.

### Input Format Interpretation
- `N D`
- `N` digits

### Output Format Interpretation
- allowed count

### Example 1 (Source-Style)
Input:
```text
6 13
2 5 8 1 7 4
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Odd-Even Vehicle Rule`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Rearrange Multiples of 10 to End (Stable)

### What Exactly This Question Is Asking
- In simple words: Given array, move all elements divisible by 10 to end while preserving order of both groups.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given array, move all elements divisible by 10 to end while preserving order of both groups.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Rearrange Multiples of 10 to End (Stable)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Push Empty Packets (0) to End

### What Exactly This Question Is Asking
- In simple words: Conveyor has packets; `0` means empty packet.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Conveyor has packets; `0` means empty packet.
Move empties to end while maintaining order of non-empty packets.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Push Empty Packets (0) to End`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Product Codes with No Repeated Digits in [L, R]

### What Exactly This Question Is Asking
- In simple words: Count numbers in range `[L, R]` having all distinct digits.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Count numbers in range `[L, R]` having all distinct digits.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Product Codes with No Repeated Digits in [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Minimum Left Rotations to Match String

### What Exactly This Question Is Asking
- In simple words: Given strings `A` and `B`, find minimum left rotations needed to make `A==B`, else `-1`.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given strings `A` and `B`, find minimum left rotations needed to make `A==B`, else `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Minimum Left Rotations to Match String`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Sort Risk Levels (0,1,2)

### What Exactly This Question Is Asking
- In simple words: Sort risk levels (0,1,2) in one pass.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Sort risk levels (0,1,2) in one pass.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Sort Risk Levels (0,1,2)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Maximum Profit from Stock Prices

### What Exactly This Question Is Asking
- In simple words: Given daily stock prices, buy once and sell once later.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given daily stock prices, buy once and sell once later. Find max profit.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Maximum Profit from Stock Prices`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Transaction Burst Count (Subarrays with Target Sum)

### What Exactly This Question Is Asking
- In simple words: Given daily gains/losses array and target `K`, count contiguous periods summing to `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given daily gains/losses array and target `K`, count contiguous periods summing to `K`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Transaction Burst Count (Subarrays with Target Sum)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. ATM Cash Dispense Simulation

### What Exactly This Question Is Asking
- In simple words: ATM has limited notes: 2000, 500, 200, 100.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
ATM has limited notes: 2000, 500, 200, 100. Given amount and stock, dispense with greedy high-to-low.
If impossible, print `-1`.

### Input Format Interpretation
- amount
- stock of 2000 500 200 100

### Output Format Interpretation
- dispensed notes count in same order or `-1`

### Example 1 (Source-Style)
Input Format
- amount
- stock of 2000 500 200 100

### Output Format
- dispensed notes count in same order or `-1`

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    long long amt;
    cin >> amt;
    vector<int> den = {2000, 500, 200, 100};

Detailed Explanation:
- Start by identifying required operation from title: `Q9. ATM Cash Dispense Simulation`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Vending Machine Change Feasibility

### What Exactly This Question Is Asking
- In simple words: Given customer queue payments for item cost 5 (pay with 5/10/20), determine if seller can provide change always.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given customer queue payments for item cost 5 (pay with 5/10/20), determine if seller can provide change always.

### Input Format Interpretation
- `N`
- `N` payments

### Output Format Interpretation
- `Yes` / `No`

### Example 1 (Source-Style)
Input Format
- `N`
- `N` payments

### Output Format
- `Yes` / `No`

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    int c5 = 0, c10 = 0;
    for (int i = 0; i < n; i++) {
        int x; cin >> x;

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Vending Machine Change Feasibility`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Time Needed for K-th Ticket Buyer

### What Exactly This Question Is Asking
- In simple words: Queue of ticket demands.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Queue of ticket demands. Front buys 1 ticket each second and requeues if still needed.
Find time for person `k` to finish.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Time Needed for K-th Ticket Buyer`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Print Queue Priority (When Target Prints)

### What Exactly This Question Is Asking
- In simple words: Jobs have priorities.
- Core solving focus: follow FIFO order and handle front/rear transitions correctly.

### Problem Understanding (Detailed)
Jobs have priorities. If front is not highest, move to back; else print.
Find print order of target index.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Print Queue Priority (When Target Prints)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Round Robin Completion Time Simulation

### What Exactly This Question Is Asking
- In simple words: Given process burst times and fixed quantum `q`, compute completion time for each process (arrival all at 0).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given process burst times and fixed quantum `q`, compute completion time for each process (arrival all at 0).

### Input Format Interpretation
- `N q`
- `N` burst times

### Output Format Interpretation
- completion times in process order

### Example 1 (Source-Style)
Input Format
- `N q`
- `N` burst times

### Output Format
- completion times in process order

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, q;
    cin >> n >> q;

    vector<int> bt(n), rem(n), ct(n, 0);
    for (int i =

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Round Robin Completion Time Simulation`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Single-Agent Call Center Waiting Time

### What Exactly This Question Is Asking
- In simple words: Customers arrive with arrival time and service time.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Customers arrive with arrival time and service time. One agent serves FIFO.
Print waiting time per customer.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Single-Agent Call Center Waiting Time`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. LRU Page Fault Simulation

### What Exactly This Question Is Asking
- In simple words: Given cache capacity and page request sequence, count page faults using LRU replacement.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given cache capacity and page request sequence, count page faults using LRU replacement.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q15. LRU Page Fault Simulation`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Browser Back/Forward Simulation

### What Exactly This Question Is Asking
- In simple words: Support commands: `VISIT url`, `BACK`, `FORWARD`, `CURRENT` from initial page `home`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Support commands: `VISIT url`, `BACK`, `FORWARD`, `CURRENT` from initial page `home`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Browser Back/Forward Simulation`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Caesar Cipher Encryption (Story)

### What Exactly This Question Is Asking
- In simple words: Encrypt message by shifting alphabetic characters by `k`, preserving case.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Encrypt message by shifting alphabetic characters by `k`, preserving case.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Caesar Cipher Encryption (Story)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Keyword Checker (Compiler Story)

### What Exactly This Question Is Asking
- In simple words: Given word, print `Keyword` if C/C++ reserved keyword else `Not Keyword`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given word, print `Keyword` if C/C++ reserved keyword else `Not Keyword`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Keyword Checker (Compiler Story)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Mask Digits Except Last 4

### What Exactly This Question Is Asking
- In simple words: In a text, replace all digits except last 4 digits by `X`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
In a text, replace all digits except last 4 digits by `X`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Mask Digits Except Last 4`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Multi-Delimiter Token Counter

### What Exactly This Question Is Asking
- In simple words: Given text and delimiter set string, count tokens separated by any delimiter.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given text and delimiter set string, count tokens separated by any delimiter.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Multi-Delimiter Token Counter`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Robot Navigation with Obstacles

### What Exactly This Question Is Asking
- In simple words: Given grid (`0` free, `1` blocked), start position and command string `L/R/U/D`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given grid (`0` free, `1` blocked), start position and command string `L/R/U/D`.
Execute commands only if next cell valid and free.
Print final coordinates.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Robot Navigation with Obstacles`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Parking Lot Occupancy Updates

### What Exactly This Question Is Asking
- In simple words: Grid has parking slots (`0` free, `1` occupied).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Grid has parking slots (`0` free, `1` occupied). Process operations:
- `PARK r c`
- `LEAVE r c`
After all operations print total occupied.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Parking Lot Occupancy Updates`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Rotting Oranges Simulation

### What Exactly This Question Is Asking
- In simple words: Grid values: `0` empty, `1` fresh, `2` rotten.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Grid values: `0` empty, `1` fresh, `2` rotten. Rotten spreads to adjacent fresh per minute.
Find time to rot all or `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Rotting Oranges Simulation`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Nearest Exit Distance for Each Cell (Multi-source BFS)

### What Exactly This Question Is Asking
- In simple words: Grid has exits as `1`, walls as `-1`, free as `0`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Grid has exits as `1`, walls as `-1`, free as `0`.
Print shortest distance from each free cell to nearest exit.
Walls remain `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Nearest Exit Distance for Each Cell (Multi-source BFS)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Flood Fill Paint Tool

### What Exactly This Question Is Asking
- In simple words: Replace connected component color from `(sr,sc)` with new color.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Replace connected component color from `(sr,sc)` with new color.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Flood Fill Paint Tool`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Island Count (Land-Water Map)

### What Exactly This Question Is Asking
- In simple words: Count islands of `1`s in binary grid using 4-direction adjacency.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count islands of `1`s in binary grid using 4-direction adjacency.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Island Count (Land-Water Map)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Electricity Bill Slab Simulation

### What Exactly This Question Is Asking
- In simple words: Billing slabs: - First 100 units: 1.5/unit - Next 100: 2.5/unit - Next 300: 4/unit - Above 500: 6/unit Add fixed charge 50.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Billing slabs:
- First 100 units: 1.5/unit
- Next 100: 2.5/unit
- Next 300: 4/unit
- Above 500: 6/unit
Add fixed charge 50.
Given units `U`, print final bill.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Electricity Bill Slab Simulation`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Taxi Fare Simulation with Night Surcharge

### What Exactly This Question Is Asking
- In simple words: Fare rules: - Base fare: 50 for first 2 km - Next 8 km: 12/km - Beyond 10 km: 10/km - If start hour in [22..23] or [0..5], add 20% surcharge on total Input distance and start hour.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Fare rules:
- Base fare: 50 for first 2 km
- Next 8 km: 12/km
- Beyond 10 km: 10/km
- If start hour in [22..23] or [0..5], add 20% surcharge on total
Input distance and start hour.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Taxi Fare Simulation with Night Surcharge`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Monthly Savings with Interest + Deposit

### What Exactly This Question Is Asking
- In simple words: Given initial balance `P`, monthly deposit `D`, monthly interest rate `%` and months `M`, simulate final balance.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given initial balance `P`, monthly deposit `D`, monthly interest rate `%` and months `M`, simulate final balance.
Interest applies on current balance at month end after deposit.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Monthly Savings with Interest + Deposit`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Inventory Reorder Simulation with Lead Time

### What Exactly This Question Is Asking
- In simple words: Given daily demand for `N` days, initial stock `S`, reorder threshold `T`, reorder quantity `Q`, lead time `L` days.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given daily demand for `N` days, initial stock `S`, reorder threshold `T`, reorder quantity `Q`, lead time `L` days.
Whenever stock after demand <= T and no pending order, place order that arrives after `L` full days.
If demand exceeds stock, unmet demand is lost.
Print total lost demand and ending stock.

### Input Format Interpretation
- `N S T Q L`
- `N` daily demand values

### Output Format Interpretation
- `lost endingStock`

### Example 1 (Source-Style)
Input Format
- `N S T Q L`
- `N` daily demand values

### Output Format
- `lost endingStock`

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

struct Order { int arriveDay; int qty; };

int main() {
    int n, s, t, q, l;
    cin >> n >> s >> t >> q >> l;
    ve

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Inventory Reorder Simulation with Lead Time`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Minimum Train Platforms Required

### What Exactly This Question Is Asking
- In simple words: Given arrival and departure times of trains (same day, HHMM integer), find minimum platforms required.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given arrival and departure times of trains (same day, HHMM integer), find minimum platforms required.

### Input Format Interpretation
- `N`
- `N` arrival times
- `N` departure times

### Output Format Interpretation
- min platforms

### Example 1 (Source-Style)
Input Format
- `N`
- `N` arrival times
- `N` departure times

### Output Format
- min platforms

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> arr(n), dep(n);
    for (auto &x : arr) cin >> x;
    for (auto &x : d

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Minimum Train Platforms Required`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. CPU Task Scheduler with Cooldown

### What Exactly This Question Is Asking
- In simple words: Tasks are uppercase letters; same task needs `n` cooldown intervals.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Tasks are uppercase letters; same task needs `n` cooldown intervals.
Find minimum total intervals to finish all tasks.

### Input Format Interpretation
- tasks string
- cooldown n

### Output Format Interpretation
- minimum intervals

### Example 1 (Source-Style)
Input Format
- tasks string
- cooldown n

### Output Format
- minimum intervals

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string tasks;
    int n;
    cin >> tasks >> n;

    vector<int> f(26, 0);
    for (char c : tasks) f[c

Detailed Explanation:
- Start by identifying required operation from title: `Q32. CPU Task Scheduler with Cooldown`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: one-step scenario with minimum entities
Walkthrough: Walkthrough: Apply rules in exact sequence and update state once.
Expected Output: state/result after one simulation step.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
