# TCS_NQT_Arrays_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Largest and Smallest Element in an Array

### What Exactly This Question Is Asking
- In simple words: Given an array of `N` integers, find the smallest and largest element.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array of `N` integers, find the smallest and largest element.

### Input Format Interpretation
- First line: integer `N`
- Second line: `N` space-separated integers

### Output Format Interpretation
Print two integers: `smallest largest`

### Constraint Notes
- `1 <= N <= 2 * 10^5`
- `-10^9 <= A[i] <= 10^9`

### Example 1 (Source-Style)
Input:
```text
5
8 3 11 -2 6
```
Output:
```text
-2 11
```

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Largest and Smallest Element in an Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Second Largest and Second Smallest (Distinct)

### What Exactly This Question Is Asking
- In simple words: Given an array, print the second smallest and second largest distinct values.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array, print the second smallest and second largest distinct values.
If either does not exist, print `-1 -1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
`secondSmallest secondLargest`

### Constraint Notes
- `1 <= N <= 2 * 10^5`

### Example 1 (Source-Style)
Input:
```text
6
7 2 9 2 11 7
```
Output:
```text
7 9
```

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Second Largest and Second Smallest (Distinct)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Check if Array is Sorted (Non-Decreasing)

### What Exactly This Question Is Asking
- In simple words: Given an array of integers, determine if it is sorted in non-decreasing order.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given an array of integers, determine if it is sorted in non-decreasing order.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print `Yes` if sorted, else `No`.

### Constraint Notes
- `1 <= N <= 2 * 10^5`

### Example 1 (Source-Style)
Input:
```text
5
1 2 2 7 9
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Check if Array is Sorted (Non-Decreasing)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Left Rotate an Array by K Places

### What Exactly This Question Is Asking
- In simple words: Given an array of size `N`, rotate it to the left by `K` positions.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array of size `N`, rotate it to the left by `K` positions.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
Print rotated array.

### Constraint Notes
- `1 <= N <= 2 * 10^5`
- `0 <= K <= 10^9`

### Example 1 (Source-Style)
Input:
```text
5 2
10 20 30 40 50
```
Output:
```text
30 40 50 10 20
```

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Left Rotate an Array by K Places`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Right Rotate an Array by K Places

### What Exactly This Question Is Asking
- In simple words: Given an array of size `N`, rotate it to the right by `K` positions.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array of size `N`, rotate it to the right by `K` positions.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
Print rotated array.

### Example 1 (Source-Style)
Input:
```text
5 2
10 20 30 40 50
```
Output:
```text
40 50 10 20 30
```

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Right Rotate an Array by K Places`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Reverse an Array

### What Exactly This Question Is Asking
- In simple words: Given an array, reverse it in-place.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array, reverse it in-place.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print reversed array.

### Example 1 (Source-Style)
Input:
```text
4
1 2 3 4
```
Output:
```text
4 3 2 1
```

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Reverse an Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Missing Number from 1 to N

### What Exactly This Question Is Asking
- In simple words: You are given `N-1` distinct numbers from `1` to `N`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
You are given `N-1` distinct numbers from `1` to `N`. Find the missing number.

### Input Format Interpretation
- First line: `N`
- Second line: `N-1` integers

### Output Format Interpretation
Print the missing number.

### Constraint Notes
- `2 <= N <= 2 * 10^5`

### Example 1 (Source-Style)
Input:
```text
5
1 2 4 5
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Missing Number from 1 to N`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Single Number (All Others Appear Twice)

### What Exactly This Question Is Asking
- In simple words: Given an array where every element appears exactly twice except one element that appears once, find the single element.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array where every element appears exactly twice except one element that appears once, find the single element.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print the single element.

### Example 1 (Source-Style)
Input:
```text
7
2 5 2 7 5 9 9
```
Output:
```text
7
```

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Single Number (All Others Appear Twice)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Story-Based: Push Empty Packets (0) to End

### What Exactly This Question Is Asking
- In simple words: A conveyor belt carries packets represented by integers.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A conveyor belt carries packets represented by integers. Empty packets are denoted by `0`.
Move all empty packets to the end while maintaining the relative order of non-zero packets.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print transformed array.

### Example 1 (Source-Style)
Input:
```text
8
4 0 3 0 0 7 5 0
```
Output:
```text
4 3 7 5 0 0 0 0
```

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Story-Based: Push Empty Packets (0) to End`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Longest Subarray with Sum K (Handles Negatives)

### What Exactly This Question Is Asking
- In simple words: Given an integer array (may include negative values), find the length of the longest subarray whose sum is exactly `K`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given an integer array (may include negative values), find the length of the longest subarray whose sum is exactly `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
Print one integer: longest length.

### Example 1 (Source-Style)
Input:
```text
8 5
1 -1 5 -2 3 2 -2 1
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Longest Subarray with Sum K (Handles Negatives)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Count Subarrays with Given Sum K

### What Exactly This Question Is Asking
- In simple words: Given an array and integer `K`, count how many subarrays have sum equal to `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given an array and integer `K`, count how many subarrays have sum equal to `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
Print count of valid subarrays.

### Example 1 (Source-Style)
Input:
```text
8 0
3 4 -7 1 3 3 1 -4
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Count Subarrays with Given Sum K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Maximum Subarray Sum (Kadane)

### What Exactly This Question Is Asking
- In simple words: Given an array, find the maximum possible sum of any non-empty contiguous subarray.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given an array, find the maximum possible sum of any non-empty contiguous subarray.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print maximum sum.

### Example 1 (Source-Style)
Input:
```text
9
-2 1 -3 4 -1 2 1 -5 4
```
Output:
```text
6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Maximum Subarray Sum (Kadane)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Two Sum Problem

### What Exactly This Question Is Asking
- In simple words: Given array `A` and target `T`, find indices of two elements whose sum is `T`.
- Core solving focus: track complements or pointers and avoid invalid duplicates.

### Problem Understanding (Detailed)
Given array `A` and target `T`, find indices of two elements whose sum is `T`.
If multiple answers exist, print the first found pair. If none, print `-1`.

### Input Format Interpretation
- First line: `N T`
- Second line: `N` integers

### Output Format Interpretation
- Print two 0-based indices `i j`, or `-1`

### Example 1 (Source-Style)
Input:
```text
5 9
2 7 11 15 1
```
Output:
```text
0 1
```

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Two Sum Problem`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Sort an Array of 0s, 1s and 2s

### What Exactly This Question Is Asking
- In simple words: Given an array containing only `0`, `1`, and `2`, sort it in linear time.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given an array containing only `0`, `1`, and `2`, sort it in linear time.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers (`0/1/2`)

### Output Format Interpretation
Sorted array.

### Example 1 (Source-Style)
Input:
```text
8
2 0 2 1 1 0 2 0
```
Output:
```text
0 0 0 1 1 2 2 2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Sort an Array of 0s, 1s and 2s`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. Majority Element (> N/2 occurrences)

### What Exactly This Question Is Asking
- In simple words: Given an array, find the majority element (appears more than `N/2` times).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array, find the majority element (appears more than `N/2` times).
If not present, print `-1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Majority value or `-1`

### Example 1 (Source-Style)
Input:
```text
7
2 2 1 2 3 2 2
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q15. Majority Element (> N/2 occurrences)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Leaders in an Array

### What Exactly This Question Is Asking
- In simple words: An element is a leader if it is greater than or equal to all elements to its right.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
An element is a leader if it is greater than or equal to all elements to its right.
Print all leaders in original order.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Leaders separated by spaces.

### Example 1 (Source-Style)
Input:
```text
6
16 17 4 3 5 2
```
Output:
```text
17 5 2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Leaders in an Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Maximum Sum Subarray of Size K (Sliding Window)

### What Exactly This Question Is Asking
- In simple words: Given an array and integer `K`, find maximum sum among all contiguous subarrays of size exactly `K`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given an array and integer `K`, find maximum sum among all contiguous subarrays of size exactly `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
Print maximum sum.

### Example 1 (Source-Style)
Input:
```text
7 3
2 1 5 1 3 2 4
```
Output:
```text
9
```

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Maximum Sum Subarray of Size K (Sliding Window)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Longest Subarray with Equal Number of 0s and 1s

### What Exactly This Question Is Asking
- In simple words: Given a binary array, find the length of the longest subarray containing equal number of `0`s and `1`s.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given a binary array, find the length of the longest subarray containing equal number of `0`s and `1`s.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers (0 or 1)

### Output Format Interpretation
Print longest length.

### Example 1 (Source-Style)
Input:
```text
8
0 0 1 0 1 1 0 1
```
Output:
```text
8
```

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Longest Subarray with Equal Number of 0s and 1s`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Story-Based: Rearrange Multiples of 10 to End (Stable)

### What Exactly This Question Is Asking
- In simple words: A supermarket marks some items with prices divisible by `10`.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
A supermarket marks some items with prices divisible by `10`. Rearrange the array so all multiples of `10` go to the end, preserving relative order in both groups.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print rearranged array.

### Example 1 (Source-Style)
Input:
```text
7
23 40 11 60 9 10 17
```
Output:
```text
23 11 9 17 40 60 10
```

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Story-Based: Rearrange Multiples of 10 to End (Stable)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Story-Based: Maximum Profit from Stock Prices

### What Exactly This Question Is Asking
- In simple words: Given stock prices for `N` days, you may buy once and sell once later.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given stock prices for `N` days, you may buy once and sell once later. Find maximum profit.
If no profit possible, print `0`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Maximum profit.

### Example 1 (Source-Style)
Input:
```text
6
7 1 5 3 6 4
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Story-Based: Maximum Profit from Stock Prices`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Count Elements Greater Than All Previous Elements

### What Exactly This Question Is Asking
- In simple words: Count how many elements are strictly greater than every element before them.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count how many elements are strictly greater than every element before them.
The first element is always counted.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Count of such elements.

### Example 1 (Source-Style)
Input:
```text
7
2 5 3 7 8 6 9
```
Output:
```text
5
```

Explanation: Valid elements are `2, 5, 7, 8, 9`.

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Count Elements Greater Than All Previous Elements`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Count Frequency of Each Element

### What Exactly This Question Is Asking
- In simple words: Given an array of `N` integers, print the frequency of each distinct element in ascending order of value.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given an array of `N` integers, print the frequency of each distinct element in ascending order of value.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
For each distinct value `x`, print one line: `x frequency`

### Constraint Notes
- `1 <= N <= 2 * 10^5`
- `-10^9 <= A[i] <= 10^9`

### Example 1 (Source-Style)
Input:
```text
6
1 2 2 3 3 3
```
Output:
```text
1 1
2 2
3 3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Count Frequency of Each Element`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Element with Maximum Frequency

### What Exactly This Question Is Asking
- In simple words: Given an array, find the element with highest frequency.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given an array, find the element with highest frequency.
If multiple elements have the same maximum frequency, print the smallest element.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print one element (mode with tie-breaker)

### Example 1 (Source-Style)
Input:
```text
8
4 1 2 2 3 3 3 1
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Element with Maximum Frequency`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Print Elements Occurring Odd and Even Number of Times

### What Exactly This Question Is Asking
- In simple words: Given an array, print elements occurring odd number of times and even number of times.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array, print elements occurring odd number of times and even number of times.
Print each group in ascending order.
If a group is empty, print `-1` for that group.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- First line: odd-frequency elements or `-1`
- Second line: even-frequency elements or `-1`

### Example 1 (Source-Style)
Input:
```text
8
1 1 2 2 2 3 3 4
```
Output:
```text
2 4
1 3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Print Elements Occurring Odd and Even Number of Times`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Remove Duplicates from Sorted Array (In-place style)

### What Exactly This Question Is Asking
- In simple words: Given a sorted array, remove duplicates in-place so that each element appears once.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given a sorted array, remove duplicates in-place so that each element appears once.
Print:
1. New length
2. The unique part of the array

### Input Format Interpretation
- First line: `N`
- Second line: `N` sorted integers

### Output Format Interpretation
- First line: new length `K`
- Second line: first `K` unique elements

### Example 1 (Source-Style)
Input:
```text
7
1 1 2 2 2 3 4
```
Output:
```text
4
1 2 3 4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Remove Duplicates from Sorted Array (In-place style)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Remove Duplicates from Unsorted Array (Preserve First Occurrence)

### What Exactly This Question Is Asking
- In simple words: Given an unsorted array, remove duplicates while preserving order of first occurrence.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given an unsorted array, remove duplicates while preserving order of first occurrence.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print deduplicated array.

### Example 1 (Source-Style)
Input:
```text
8
4 2 4 1 2 3 1 5
```
Output:
```text
4 2 1 3 5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Remove Duplicates from Unsorted Array (Preserve First Occurrence)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Count Unique Elements

### What Exactly This Question Is Asking
- In simple words: Given an array, count how many distinct elements are present.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given an array, count how many distinct elements are present.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print one integer: count of unique elements.

### Example 1 (Source-Style)
Input:
```text
7
1 2 2 3 3 3 4
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Count Unique Elements`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Maximum Difference A[j] - A[i] with j > i

### What Exactly This Question Is Asking
- In simple words: Given an array, find maximum value of `A[j] - A[i]` such that `j > i`.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given an array, find maximum value of `A[j] - A[i]` such that `j > i`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print maximum difference.

### Constraint Notes
- `2 <= N <= 2 * 10^5`

### Example 1 (Source-Style)
Input:
```text
6
7 1 5 3 6 4
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Maximum Difference A[j] - A[i] with j > i`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Minimum Absolute Difference in an Array

### What Exactly This Question Is Asking
- In simple words: Given an array, find minimum absolute difference between any two elements.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given an array, find minimum absolute difference between any two elements.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print minimum absolute difference.

### Constraint Notes
- `2 <= N <= 2 * 10^5`

### Example 1 (Source-Style)
Input:
```text
5
4 9 1 32 13
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Minimum Absolute Difference in an Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Closest Pair from Two Arrays (Minimum Absolute Difference)

### What Exactly This Question Is Asking
- In simple words: Given arrays `A` of size `N` and `B` of size `M`, find pair `(A[i], B[j])` with minimum `|A[i] - B[j]|`.
- Core solving focus: track complements or pointers and avoid invalid duplicates.

### Problem Understanding (Detailed)
Given arrays `A` of size `N` and `B` of size `M`, find pair `(A[i], B[j])` with minimum `|A[i] - B[j]|`.
Print the pair and difference.
If multiple pairs exist, any one is acceptable.

### Input Format Interpretation
- First line: `N M`
- Second line: `N` integers
- Third line: `M` integers

### Output Format Interpretation
Print: `a b diff`

### Example 1 (Source-Style)
Input:
```text
4 4
1 4 10 20
5 8 15 30
```
Output:
```text
4 5 1
```

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Closest Pair from Two Arrays (Minimum Absolute Difference)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Rearrange: Negatives Left, Non-Negatives Right

### What Exactly This Question Is Asking
- In simple words: Given an array, move all negative numbers to left side and non-negative numbers to right side.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given an array, move all negative numbers to left side and non-negative numbers to right side.
Relative order is not required.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print rearranged array.

### Example 1 (Source-Style)
Input:
```text
5
-1 2 -3 4 5
```
Output (one valid):
```text
-1 -3 2 4 5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Rearrange: Negatives Left, Non-Negatives Right`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. Rearrange: Even Numbers First, Then Odd (Stable)

### What Exactly This Question Is Asking
- In simple words: Given an array, print all even numbers first, then odd numbers.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given an array, print all even numbers first, then odd numbers.
Preserve relative order inside each group.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Rearranged array.

### Example 1 (Source-Style)
Input:
```text
7
5 2 8 3 6 1 4
```
Output:
```text
2 8 6 4 5 3 1
```

Detailed Explanation:
- Start by identifying required operation from title: `Q32. Rearrange: Even Numbers First, Then Odd (Stable)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q33. Rearrange Alternating Positive and Negative (Stable)

### What Exactly This Question Is Asking
- In simple words: Given an array, rearrange it in alternating positive and negative order while preserving relative order.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given an array, rearrange it in alternating positive and negative order while preserving relative order.
If one type remains extra, append extras at the end.
Use non-negative as positive group.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Rearranged array.

### Example 1 (Source-Style)
Input:
```text
8
-1 2 -3 4 5 -6 -7 8
```
Output (one valid):
```text
-1 2 -3 4 -6 5 -7 8
```

Detailed Explanation:
- Start by identifying required operation from title: `Q33. Rearrange Alternating Positive and Negative (Stable)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q34. Build Prefix Maximum and Suffix Maximum Arrays

### What Exactly This Question Is Asking
- In simple words: Given array `A` of size `N`, build: - `prefixMax[i]` = maximum from `A[0..i]` - `suffixMax[i]` = maximum from `A[i..N-1]`
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given array `A` of size `N`, build:
- `prefixMax[i]` = maximum from `A[0..i]`
- `suffixMax[i]` = maximum from `A[i..N-1]`

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
- First line: prefix max array
- Second line: suffix max array

### Example 1 (Source-Style)
Input:
```text
6
3 1 4 2 5 0
```
Output:
```text
3 3 4 4 5 5
5 5 5 5 5 0
```

Detailed Explanation:
- Start by identifying required operation from title: `Q34. Build Prefix Maximum and Suffix Maximum Arrays`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q35. Trapping Rain Water (Prefix/Suffix Max Method)

### What Exactly This Question Is Asking
- In simple words: Given non-negative integer heights of bars, compute how much water can be trapped.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given non-negative integer heights of bars, compute how much water can be trapped.

### Input Format Interpretation
- First line: `N`
- Second line: `N` non-negative integers

### Output Format Interpretation
Print trapped water units.

### Example 1 (Source-Style)
Input:
```text
12
0 1 0 2 1 0 1 3 2 1 2 1
```
Output:
```text
6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q35. Trapping Rain Water (Prefix/Suffix Max Method)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q36. Range Query Pattern: Maximum Element Outside [L, R]

### What Exactly This Question Is Asking
- In simple words: Given array `A` and `Q` queries.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given array `A` and `Q` queries. For each query `[L, R]` (0-indexed), return maximum element outside that subarray.
If no elements remain outside, print `-1`.

### Input Format Interpretation
- First line: `N Q`
- Second line: `N` integers
- Next `Q` lines: `L R`

### Output Format Interpretation
For each query print answer on new line.

### Example 1 (Source-Style)
Input:
```text
7 3
5 1 9 3 7 2 8
1 3
0 6
4 5
```
Output:
```text
8
-1
9
```

Detailed Explanation:
- Start by identifying required operation from title: `Q36. Range Query Pattern: Maximum Element Outside [L, R]`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q37. Edge-Case Heavy: Maximum Subarray Sum with Indices

### What Exactly This Question Is Asking
- In simple words: Given an array (possibly all negative), find: 1.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given an array (possibly all negative), find:
1. Maximum subarray sum
2. Starting index
3. Ending index

If multiple subarrays have same max sum, print earliest one.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Print: `maxSum startIndex endIndex`

### Constraint Notes
- `1 <= N <= 10^6`
- `-10^9 <= A[i] <= 10^9`

### Example 1 (Source-Style)
Input:
```text
9
-2 1 -3 4 -1 2 1 -5 4
```
Output:
```text
6 3 6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q37. Edge-Case Heavy: Maximum Subarray Sum with Indices`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `n = 1, arr = [42]`
Walkthrough: Walkthrough: This checks smallest-size behavior where many loops run once.
Expected Output: base-case result for a single-element array.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
