# TCS_NQT_Hashing_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Count Frequency of Each Element

### What Exactly This Question Is Asking
- In simple words: Given an array of `N` integers, print frequency of each distinct element in ascending order of value.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given an array of `N` integers, print frequency of each distinct element in ascending order of value.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Each line: `value frequency`

### Constraint Notes
- `1 <= N <= 2 * 10^5`

### Example 1 (Source-Style)
Input:
```text
6
1 2 2 3 3 3
```
Output:
```text
1 1
2 2
3 3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Count Frequency of Each Element`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Element with Maximum Frequency

### What Exactly This Question Is Asking
- In simple words: Find the element with maximum frequency.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Find the element with maximum frequency. If tie, print smallest element.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
8
4 1 2 2 3 3 3 1
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Element with Maximum Frequency`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. First Non-Repeating Element in Array

### What Exactly This Question Is Asking
- In simple words: Given an array, find first element that appears exactly once.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array, find first element that appears exactly once. If none, print `-1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
7
4 5 1 2 1 4 5
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q3. First Non-Repeating Element in Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. First Repeating Element (By First Occurrence)

### What Exactly This Question Is Asking
- In simple words: Given an array, find the first element whose frequency is more than 1.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given an array, find the first element whose frequency is more than 1.
If none, print `-1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
7
10 5 3 4 3 5 6
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q4. First Repeating Element (By First Occurrence)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Print Elements Occurring Exactly K Times

### What Exactly This Question Is Asking
- In simple words: Given array and integer `K`, print all elements with frequency exactly `K` in ascending order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array and integer `K`, print all elements with frequency exactly `K` in ascending order.
If none, print `-1`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
Elements in ascending order or `-1`

### Example 1 (Source-Style)
Input:
```text
8 2
1 2 2 3 3 3 4 4
```
Output:
```text
2 4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Print Elements Occurring Exactly K Times`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Contains Duplicate

### What Exactly This Question Is Asking
- In simple words: Given array, check if any value appears at least twice.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array, check if any value appears at least twice.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
`Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
5
1 2 3 4 1
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Contains Duplicate`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Count Distinct Elements

### What Exactly This Question Is Asking
- In simple words: Given an array, count number of unique elements.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given an array, count number of unique elements.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
7
1 2 2 3 3 3 4
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Count Distinct Elements`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Two Sum (Return 0-based Indices)

### What Exactly This Question Is Asking
- In simple words: Given array and target `T`, find two indices `i, j` such that `A[i] + A[j] = T`.
- Core solving focus: track complements or pointers and avoid invalid duplicates.

### Problem Understanding (Detailed)
Given array and target `T`, find two indices `i, j` such that `A[i] + A[j] = T`.
If none, print `-1`.

### Input Format Interpretation
- First line: `N T`
- Second line: `N` integers

### Output Format Interpretation
`i j` or `-1`

### Example 1 (Source-Style)
Input:
```text
5 9
2 7 11 15 1
```
Output:
```text
0 1
```

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Two Sum (Return 0-based Indices)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Count Pairs with Given Sum

### What Exactly This Question Is Asking
- In simple words: Count number of unordered index pairs `(i, j)` with `i < j` and `A[i] + A[j] = K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count number of unordered index pairs `(i, j)` with `i < j` and `A[i] + A[j] = K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
6 6
1 5 7 -1 5 1
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Count Pairs with Given Sum`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Count Pairs with Absolute Difference K

### What Exactly This Question Is Asking
- In simple words: Count number of unordered index pairs `(i, j)` with `i < j` and `|A[i] - A[j]| = K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count number of unordered index pairs `(i, j)` with `i < j` and `|A[i] - A[j]| = K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
6 2
1 5 3 4 2 3
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Count Pairs with Absolute Difference K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Count Triplets with Sum K (Hash-assisted O(n^2))

### What Exactly This Question Is Asking
- In simple words: Count number of index triplets `(i, j, k)` with `i < j < k` and `A[i] + A[j] + A[k] = T`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count number of index triplets `(i, j, k)` with `i < j < k` and `A[i] + A[j] + A[k] = T`.

### Input Format Interpretation
- First line: `N T`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
6 6
1 2 3 0 3 2
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Count Triplets with Sum K (Hash-assisted O(n^2))`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Four Sum Existence (Distinct Indices)

### What Exactly This Question Is Asking
- In simple words: Given array and target `T`, determine if there exist 4 distinct indices whose values sum to `T`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array and target `T`, determine if there exist 4 distinct indices whose values sum to `T`.
Print `Yes` or `No`.

### Input Format Interpretation
- First line: `N T`
- Second line: `N` integers

### Output Format Interpretation
`Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
6 17
3 1 4 6 5 9
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Four Sum Existence (Distinct Indices)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Count Subarrays with Sum K

### What Exactly This Question Is Asking
- In simple words: Given array (can contain negative values), count subarrays with sum exactly `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given array (can contain negative values), count subarrays with sum exactly `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
8 0
3 4 -7 1 3 3 1 -4
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Count Subarrays with Sum K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Longest Subarray with Sum K

### What Exactly This Question Is Asking
- In simple words: Given array (with negatives), find longest subarray length with sum `K`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given array (with negatives), find longest subarray length with sum `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
7 5
1 -1 5 -2 3 2 -2
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Longest Subarray with Sum K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. Zero-Sum Subarray Exists?

### What Exactly This Question Is Asking
- In simple words: Check if any non-empty subarray has sum `0`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Check if any non-empty subarray has sum `0`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
`Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
5
4 2 -3 1 6
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q15. Zero-Sum Subarray Exists?`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Count Zero-Sum Subarrays

### What Exactly This Question Is Asking
- In simple words: Count total number of subarrays having sum `0`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count total number of subarrays having sum `0`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
5
1 -1 0 2 -2
```
Output:
```text
6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Count Zero-Sum Subarrays`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Longest Zero-Sum Subarray

### What Exactly This Question Is Asking
- In simple words: Find length of longest subarray with sum `0`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Find length of longest subarray with sum `0`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
8
15 -2 2 -8 1 7 10 23
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Longest Zero-Sum Subarray`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Longest Subarray with Equal 0 and 1

### What Exactly This Question Is Asking
- In simple words: Given binary array, find longest subarray containing equal numbers of `0` and `1`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given binary array, find longest subarray containing equal numbers of `0` and `1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` values (0/1)

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
8
0 0 1 0 1 1 0 1
```
Output:
```text
8
```

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Longest Subarray with Equal 0 and 1`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Count Subarrays with Equal 0 and 1

### What Exactly This Question Is Asking
- In simple words: Given binary array, count number of subarrays having equal `0` and `1`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given binary array, count number of subarrays having equal `0` and `1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` values (0/1)

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
4
0 1 0 1
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Count Subarrays with Equal 0 and 1`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Count Subarrays with Sum Divisible by K

### What Exactly This Question Is Asking
- In simple words: Given array and integer `K`, count subarrays with sum divisible by `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given array and integer `K`, count subarrays with sum divisible by `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
6 5
4 5 0 -2 -3 1
```
Output:
```text
7
```

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Count Subarrays with Sum Divisible by K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Longest Subarray with Sum Divisible by K

### What Exactly This Question Is Asking
- In simple words: Find longest subarray length where sum is divisible by `K`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Find longest subarray length where sum is divisible by `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
6 3
2 7 6 1 4 5
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Longest Subarray with Sum Divisible by K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Longest Consecutive Sequence

### What Exactly This Question Is Asking
- In simple words: Given unsorted array, return length of longest consecutive element sequence.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given unsorted array, return length of longest consecutive element sequence.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
6
100 4 200 1 3 2
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Longest Consecutive Sequence`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Intersection of Two Arrays (Unique)

### What Exactly This Question Is Asking
- In simple words: Given arrays `A` and `B`, print unique intersection elements in ascending order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given arrays `A` and `B`, print unique intersection elements in ascending order.

### Input Format Interpretation
- First line: `N M`
- Second line: `N` integers
- Third line: `M` integers

### Output Format Interpretation
Intersection unique sorted; `-1` if empty

### Example 1 (Source-Style)
Input:
```text
5 5
1 2 2 3 4
2 2 3 5 6
```
Output:
```text
2 3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Intersection of Two Arrays (Unique)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Intersection of Two Arrays (With Multiplicity)

### What Exactly This Question Is Asking
- In simple words: Given arrays `A` and `B`, print intersection considering frequency.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given arrays `A` and `B`, print intersection considering frequency.

### Input Format Interpretation
- First line: `N M`
- Second line: `N` integers
- Third line: `M` integers

### Output Format Interpretation
Intersection with multiplicity (any order)

### Example 1 (Source-Style)
Input:
```text
4 5
1 2 2 1
2 2 1 3 1
```
Output:
```text
1 1 2 2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Intersection of Two Arrays (With Multiplicity)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Count Elements in Union of Two Arrays

### What Exactly This Question Is Asking
- In simple words: Given arrays `A` and `B`, return count of distinct elements in union.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given arrays `A` and `B`, return count of distinct elements in union.

### Input Format Interpretation
- First line: `N M`
- Second line: `N` integers
- Third line: `M` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
5 4
1 2 3 4 5
1 2 3 6
```
Output:
```text
6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Count Elements in Union of Two Arrays`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Valid Anagram

### What Exactly This Question Is Asking
- In simple words: Given two strings `S` and `T`, check if `T` is an anagram of `S`.
- Core solving focus: compare character counts/frequencies exactly.

### Problem Understanding (Detailed)
Given two strings `S` and `T`, check if `T` is an anagram of `S`.

### Input Format Interpretation
- Line 1: `S`
- Line 2: `T`

### Output Format Interpretation
`Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
anagram
nagaram
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Valid Anagram`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s1 = "listen", s2 = "silent"`
Walkthrough: Walkthrough: Frequency of each character matches exactly.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Group Anagrams

### What Exactly This Question Is Asking
- In simple words: Group anagrams together and print each group in a new line.
- Core solving focus: compare character counts/frequencies exactly.

### Problem Understanding (Detailed)
Group anagrams together and print each group in a new line.

### Input Format Interpretation
- First line: `N`
- Next `N` lines: strings

### Output Format Interpretation
Each line a group

### Example 1 (Source-Style)
Input:
```text
6
eat
tea
tan
ate
nat
bat
```
Output (one valid):
```text
eat tea ate
tan nat
bat
```

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Group Anagrams`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s1 = "listen", s2 = "silent"`
Walkthrough: Walkthrough: Frequency of each character matches exactly.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Isomorphic Strings

### What Exactly This Question Is Asking
- In simple words: Two strings are isomorphic if characters in `S` can be replaced to get `T` with one-to-one mapping.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Two strings are isomorphic if characters in `S` can be replaced to get `T` with one-to-one mapping.

### Input Format Interpretation
- Line 1: `S`
- Line 2: `T`

### Output Format Interpretation
`Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
egg
add
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Isomorphic Strings`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Word Pattern Matching

### What Exactly This Question Is Asking
- In simple words: Given a pattern string and a sentence, check if sentence words follow the same one-to-one mapping pattern.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given a pattern string and a sentence, check if sentence words follow the same one-to-one mapping pattern.

### Input Format Interpretation
- Line 1: pattern (e.g., `abba`)
- Line 2: sentence (space-separated words)

### Output Format Interpretation
`Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
abba
dog cat cat dog
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Word Pattern Matching`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Top K Frequent Elements

### What Exactly This Question Is Asking
- In simple words: Given array and integer `K`, print `K` most frequent elements.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array and integer `K`, print `K` most frequent elements.
Tie-break: smaller value first.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
`K` elements in required order

### Example 1 (Source-Style)
Input:
```text
8 2
1 1 1 2 2 3 3 3
```
Output:
```text
1 3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Top K Frequent Elements`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Minimum Window Substring

### What Exactly This Question Is Asking
- In simple words: Given strings `S` and `T`, find minimum substring of `S` containing all characters of `T`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given strings `S` and `T`, find minimum substring of `S` containing all characters of `T`.
If impossible, print `-1`.

### Input Format Interpretation
- Line 1: `S`
- Line 2: `T`

### Output Format Interpretation
Minimum window or `-1`

### Example 1 (Source-Style)
Input:
```text
ADOBECODEBANC
ABC
```
Output:
```text
BANC
```

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Minimum Window Substring`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. Find All Anagram Start Indices

### What Exactly This Question Is Asking
- In simple words: Given text `S` and pattern `P`, find all start indices where an anagram of `P` appears.
- Core solving focus: compare character counts/frequencies exactly.

### Problem Understanding (Detailed)
Given text `S` and pattern `P`, find all start indices where an anagram of `P` appears.
If none, print `-1`.

### Input Format Interpretation
- Line 1: `S`
- Line 2: `P`

### Output Format Interpretation
Indices space-separated or `-1`

### Example 1 (Source-Style)
Input:
```text
cbaebabacd
abc
```
Output:
```text
0 6
```

Detailed Explanation:
- Start by identifying required operation from title: `Q32. Find All Anagram Start Indices`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s1 = "listen", s2 = "silent"`
Walkthrough: Walkthrough: Frequency of each character matches exactly.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q33. Distinct Elements in Every Window of Size K

### What Exactly This Question Is Asking
- In simple words: Given array and window size `K`, print number of distinct elements in each window.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given array and window size `K`, print number of distinct elements in each window.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
Counts for each window

### Example 1 (Source-Style)
Input:
```text
7 4
1 2 1 3 4 2 3
```
Output:
```text
3 4 4 3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q33. Distinct Elements in Every Window of Size K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q34. Longest Substring Without Repeating Characters

### What Exactly This Question Is Asking
- In simple words: Given string, find length of longest substring with all distinct characters.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given string, find length of longest substring with all distinct characters.

### Input Format Interpretation
- One line string

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
abcabcbb
```
Output:
```text
3
```

Detailed Explanation:
- Start by identifying required operation from title: `Q34. Longest Substring Without Repeating Characters`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q35. Smallest Subarray Containing All Distinct Elements of Array

### What Exactly This Question Is Asking
- In simple words: Given array, find length of smallest subarray that contains every distinct element present in the full array.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given array, find length of smallest subarray that contains every distinct element present in the full array.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
8
7 3 7 3 1 3 4 1
```
Output:
```text
5
```

Detailed Explanation:
- Start by identifying required operation from title: `Q35. Smallest Subarray Containing All Distinct Elements of Array`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q36. Ransom Note Construction

### What Exactly This Question Is Asking
- In simple words: Given strings `ransom` and `magazine`, check if ransom can be constructed from magazine characters.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given strings `ransom` and `magazine`, check if ransom can be constructed from magazine characters.
Each character can be used once.

### Input Format Interpretation
- Line 1: ransom
- Line 2: magazine

### Output Format Interpretation
`Yes` / `No`

### Example 1 (Source-Style)
Input:
```text
aa
aab
```
Output:
```text
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q36. Ransom Note Construction`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q37. Majority Elements More Than n/3 (Hashing Approach)

### What Exactly This Question Is Asking
- In simple words: Given array, print all elements appearing more than `n/3` times in ascending order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given array, print all elements appearing more than `n/3` times in ascending order.
If none, print `-1`.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Elements or `-1`

### Example 1 (Source-Style)
Input:
```text
7
1 2 3 1 1 2 1
```
Output:
```text
1
```

Detailed Explanation:
- Start by identifying required operation from title: `Q37. Majority Elements More Than n/3 (Hashing Approach)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q38. Count Subarrays with XOR = K

### What Exactly This Question Is Asking
- In simple words: Given array and integer `K`, count number of subarrays with XOR equal to `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given array and integer `K`, count number of subarrays with XOR equal to `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
5 6
4 2 2 6 4
```
Output:
```text
4
```

Detailed Explanation:
- Start by identifying required operation from title: `Q38. Count Subarrays with XOR = K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q39. Longest Subarray with XOR = K

### What Exactly This Question Is Asking
- In simple words: Given array and integer `K`, find length of longest subarray with XOR equal to `K`.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Given array and integer `K`, find length of longest subarray with XOR equal to `K`.

### Input Format Interpretation
- First line: `N K`
- Second line: `N` integers

### Output Format Interpretation
One integer

### Example 1 (Source-Style)
Input:
```text
6 5
5 6 7 8 9 10
```
Output:
```text
2
```

Detailed Explanation:
- Start by identifying required operation from title: `Q39. Longest Subarray with XOR = K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q40. Story-Based: Daily Transaction Anomaly (Pair Sum Alert)

### What Exactly This Question Is Asking
- In simple words: A fraud system flags a day if two transactions sum to suspicious amount `S`.
- Core solving focus: track complements or pointers and avoid invalid duplicates.

### Problem Understanding (Detailed)
A fraud system flags a day if two transactions sum to suspicious amount `S`.
Given `N` transaction values and `S`, print `ALERT` if any pair exists, else `SAFE`.

### Input Format Interpretation
- First line: `N S`
- Second line: `N` integers

### Output Format Interpretation
`ALERT` / `SAFE`

### Example 1 (Source-Style)
Input:
```text
6 100
20 30 70 15 85 40
```
Output:
```text
ALERT
```

Detailed Explanation:
- Start by identifying required operation from title: `Q40. Story-Based: Daily Transaction Anomaly (Pair Sum Alert)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q41. Story-Based: Find Duplicate Employee ID in Log Stream

### What Exactly This Question Is Asking
- In simple words: A log stream contains employee IDs; exactly one ID appears twice and others once.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
A log stream contains employee IDs; exactly one ID appears twice and others once.
Find the duplicate ID in one pass using hashing.

### Input Format Interpretation
- First line: `N`
- Second line: `N` integers

### Output Format Interpretation
Duplicate ID or `-1`

### Example 1 (Source-Style)
Input:
```text
7
101 204 330 101 405 506 607
```
Output:
```text
101
```

Detailed Explanation:
- Start by identifying required operation from title: `Q41. Story-Based: Find Duplicate Employee ID in Log Stream`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q42. Design: LRU Cache (HashMap + Doubly Linked List)

### What Exactly This Question Is Asking
- In simple words: Implement LRU cache with: - `get(key)` returns value or `-1` - `put(key, value)` inserts/updates key When capacity exceeds, evict least recently used key.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Implement LRU cache with:
- `get(key)` returns value or `-1`
- `put(key, value)` inserts/updates key
When capacity exceeds, evict least recently used key.

### Input Format Interpretation
- First line: `capacity q`
- Next `q` lines operations:
  - `GET key`
  - `PUT key value`

### Output Format Interpretation
For each `GET`, print result.

### Example 1 (Source-Style)
Input:
```text
2 6
PUT 1 10
PUT 2 20
GET 1
PUT 3 30
GET 2
GET 3
```
Output:
```text
10
-1
30
```

Detailed Explanation:
- Start by identifying required operation from title: `Q42. Design: LRU Cache (HashMap + Doubly Linked List)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q43. Design: HashSet Operations

### What Exactly This Question Is Asking
- In simple words: Process operations on integer set: - `ADD x` - `REMOVE x` - `CONTAINS x` Print `Yes/No` for contains queries.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Process operations on integer set:
- `ADD x`
- `REMOVE x`
- `CONTAINS x`
Print `Yes/No` for contains queries.

### Input Format Interpretation
- First line: `Q`
- Next `Q` lines: operation

### Output Format Interpretation
For each `CONTAINS`, print `Yes` or `No`

### Example 1 (Source-Style)
Input:
```text
6
ADD 5
ADD 10
CONTAINS 5
REMOVE 5
CONTAINS 5
CONTAINS 10
```
Output:
```text
Yes
No
Yes
```

Detailed Explanation:
- Start by identifying required operation from title: `Q43. Design: HashSet Operations`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: small hand-checkable values
Walkthrough: Walkthrough: Track intermediate state updates at each step to validate logic.
Expected Output: deterministic result for this compact case.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
