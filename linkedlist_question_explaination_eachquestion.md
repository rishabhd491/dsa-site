# TCS_NQT_LinkedList_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Insert at Head (Singly Linked List)

### What Exactly This Question Is Asking
- In simple words: Insert given value at head of list.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Insert given value at head of list.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Insert at Head (Singly Linked List)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Insert at Tail

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Insert at Tail`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Insert at Position (1-based)

### What Exactly This Question Is Asking
- In simple words: Insert value `x` at position `pos` (1-based).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Insert value `x` at position `pos` (1-based). If `pos > length+1`, ignore.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Insert at Position (1-based)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Delete Node by Position

### What Exactly This Question Is Asking
- In simple words: Delete node at 1-based position `pos`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Delete node at 1-based position `pos`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Delete Node by Position`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. Search in Linked List

### What Exactly This Question Is Asking
- In simple words: Given key `x`, return 1-based position of first occurrence, else `-1`.
- Core solving focus: maintain valid search space and update low/high boundaries correctly.

### Problem Understanding (Detailed)
Given key `x`, return 1-based position of first occurrence, else `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q5. Search in Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [1,3,5,7,9], target = 7`
Walkthrough: Walkthrough: Mid comparison narrows search to target index.
Expected Output: index/value found indicator as required by the question.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Reverse Linked List (Iterative)

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Reverse Linked List (Iterative)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Reverse Linked List (Recursive)

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Reverse Linked List (Recursive)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Middle of Linked List

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Middle of Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Remove N-th Node from End

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Remove N-th Node from End`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Merge Two Sorted Linked Lists

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Merge Two Sorted Linked Lists`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Detect Cycle in Linked List

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Detect Cycle in Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Find Start of Cycle

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Find Start of Cycle`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Remove Cycle from Linked List

### What Exactly This Question Is Asking
- In simple words: Given list with possible cycle (pos), remove cycle and print linear list.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Given list with possible cycle (pos), remove cycle and print linear list.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Remove Cycle from Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Palindrome Linked List

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: identify symmetry logic and compare mirrored positions.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Palindrome Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s = "abba"`
Walkthrough: Walkthrough: Compare first-last and inward pairs; all pairs match.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. Intersection Point of Two Linked Lists

### What Exactly This Question Is Asking
- In simple words: Given two linked lists that may intersect, return intersection node value or `-1`.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Given two linked lists that may intersect, return intersection node value or `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q15. Intersection Point of Two Linked Lists`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Add Two Numbers (Digits in Reverse Order)

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Add Two Numbers (Digits in Reverse Order)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Odd Even Linked List

### What Exactly This Question Is Asking
- In simple words: Group odd-indexed nodes followed by even-indexed nodes.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Group odd-indexed nodes followed by even-indexed nodes.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Odd Even Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Remove Duplicates from Sorted Linked List

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Remove Duplicates from Sorted Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Remove Duplicates from Unsorted Linked List

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Remove Duplicates from Unsorted Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Segregate 0s, 1s, 2s in Linked List

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Segregate 0s, 1s, 2s in Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Rotate Linked List Right by K

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Rotate Linked List Right by K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Swap Nodes in Pairs

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: track complements or pointers and avoid invalid duplicates.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Swap Nodes in Pairs`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Reverse Nodes in K-Group

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Reverse Nodes in K-Group`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Partition List Around Value X

### What Exactly This Question Is Asking
- In simple words: Reorder list so nodes `< x` come before nodes `>= x` preserving original order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Reorder list so nodes `< x` come before nodes `>= x` preserving original order.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Partition List Around Value X`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Sort Linked List (Merge Sort)

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Sort Linked List (Merge Sort)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Copy Linked List with Random Pointer

### What Exactly This Question Is Asking
- In simple words: Each node has `next` and `random`.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Each node has `next` and `random`. Create deep copy.
Input format simplified as arrays.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Copy Linked List with Random Pointer`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Flatten Sorted Multi-Level Linked List (Bottom Pointers)

### What Exactly This Question Is Asking
- In simple words: Given list where each node has `next` and sorted `bottom` list, flatten to one sorted bottom list.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Given list where each node has `next` and sorted `bottom` list, flatten to one sorted bottom list.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Flatten Sorted Multi-Level Linked List (Bottom Pointers)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Doubly Linked List Insert at Head and Tail

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Doubly Linked List Insert at Head and Tail`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Reverse Doubly Linked List

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Reverse Doubly Linked List`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. LRU Cache (HashMap + Doubly Linked List)

### What Exactly This Question Is Asking
- In simple words: Design LRU cache with operations `GET key` and `PUT key value`.
- Core solving focus: update node links in correct order and keep head/tail references valid.

### Problem Understanding (Detailed)
Design LRU cache with operations `GET key` and `PUT key value`.

### Input Format Interpretation
- `capacity Q`
- Q lines operations

### Output Format Interpretation
- print result for each `GET`

### Example 1 (Source-Style)
Input Format
- `capacity Q`
- Q lines operations

### Output Format
- print result for each `GET`

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

class LRUCache {
    int cap;
    list<pair<int,int>> dq; // front is most recent
    unordered_map<int, list<

Detailed Explanation:
- Start by identifying required operation from title: `Q30. LRU Cache (HashMap + Doubly Linked List)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `head = 10 -> NULL`
Walkthrough: Walkthrough: One-node list verifies pointer updates without breaking links.
Expected Output: structurally valid linked list after operation.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
