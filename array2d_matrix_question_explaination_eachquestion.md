# TCS_NQT_2D_Array_Matrix_Full_Questions_Answers_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Sum of All Elements in Matrix

### What Exactly This Question Is Asking
- In simple words: Given `R x C` matrix, find sum of all elements.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Given `R x C` matrix, find sum of all elements.

### Input Format Interpretation
- `R C`
- `R` lines each with `C` integers

### Output Format Interpretation
- total sum

### Example 1 (Source-Style)
Input:
```text
2 3
1 2 3
4 5 6
```
Output:
```text
21
```

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Sum of All Elements in Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Row-wise and Column-wise Sum

### What Exactly This Question Is Asking
- In simple words: Print sum of each row and each column.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Print sum of each row and each column.

### Input Format Interpretation
- `R C`
- matrix elements

### Output Format Interpretation
- First line: row sums
- Second line: column sums

### Example 1 (Source-Style)
Input Format
- `R C`
- matrix elements

### Output Format
- First line: row sums
- Second line: column sums

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int r, c;
    cin >> r >> c;

    vector<vector<long long>> a(r, vector<l

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Row-wise and Column-wise Sum`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Largest and Smallest Element in Matrix

### What Exactly This Question Is Asking
- In simple words: Find minimum and maximum element in a matrix.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Find minimum and maximum element in a matrix.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Largest and Smallest Element in Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Matrix Transpose

### What Exactly This Question Is Asking
- In simple words: Given matrix `R x C`, print transpose `C x R`.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Given matrix `R x C`, print transpose `C x R`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Matrix Transpose`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. In-Place Transpose of Square Matrix

### What Exactly This Question Is Asking
- In simple words: Transpose an `N x N` matrix in-place.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Transpose an `N x N` matrix in-place.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q5. In-Place Transpose of Square Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. Rotate Matrix 90 Degree Clockwise

### What Exactly This Question Is Asking
- In simple words: Rotate an `N x N` matrix by 90 degrees clockwise in-place.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Rotate an `N x N` matrix by 90 degrees clockwise in-place.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q6. Rotate Matrix 90 Degree Clockwise`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Rotate Matrix 90 Degree Anti-Clockwise

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Rotate Matrix 90 Degree Anti-Clockwise`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Spiral Traversal

### What Exactly This Question Is Asking
- In simple words: Print elements of matrix in spiral order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Print elements of matrix in spiral order.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Spiral Traversal`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. Boundary Traversal

### What Exactly This Question Is Asking
- In simple words: Print boundary elements of matrix in clockwise order.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Print boundary elements of matrix in clockwise order.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q9. Boundary Traversal`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Main and Secondary Diagonal Sum

### What Exactly This Question Is Asking
- In simple words: For square matrix, compute main diagonal sum and secondary diagonal sum.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
For square matrix, compute main diagonal sum and secondary diagonal sum.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Main and Secondary Diagonal Sum`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Check Symmetric Matrix

### What Exactly This Question Is Asking
- In simple words: Check if a square matrix is symmetric (`A == A^T`).
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Check if a square matrix is symmetric (`A == A^T`).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Check Symmetric Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Check Identity Matrix

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Check Identity Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Matrix Addition

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Matrix Addition`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Matrix Multiplication

### What Exactly This Question Is Asking
- In simple words: Multiply `A (R1 x C1)` and `B (R2 x C2)` if `C1 == R2`.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Multiply `A (R1 x C1)` and `B (R2 x C2)` if `C1 == R2`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Matrix Multiplication`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. Search in Row-Wise and Column-Wise Sorted Matrix

### What Exactly This Question Is Asking
- In simple words: Matrix is sorted in rows and columns.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Matrix is sorted in rows and columns. Search target `x` and print `Yes/No`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q15. Search in Row-Wise and Column-Wise Sorted Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [1,3,5,7,9], target = 7`
Walkthrough: Walkthrough: Mid comparison narrows search to target index.
Expected Output: index/value found indicator as required by the question.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Search in Fully Sorted Matrix (Binary Search)

### What Exactly This Question Is Asking
- In simple words: Matrix rows are sorted and first element of each row > last of previous row.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Matrix rows are sorted and first element of each row > last of previous row.
Search target.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Search in Fully Sorted Matrix (Binary Search)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [1,3,5,7,9], target = 7`
Walkthrough: Walkthrough: Mid comparison narrows search to target index.
Expected Output: index/value found indicator as required by the question.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Set Matrix Zeroes

### What Exactly This Question Is Asking
- In simple words: If any cell is zero, set its entire row and column to zero.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
If any cell is zero, set its entire row and column to zero.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Set Matrix Zeroes`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Matrix Reshape

### What Exactly This Question Is Asking
- In simple words: Reshape matrix `R x C` to `r x c` preserving row-order if possible else print original.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Reshape matrix `R x C` to `r x c` preserving row-order if possible else print original.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Matrix Reshape`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Prefix Sum Matrix Build

### What Exactly This Question Is Asking
- In simple words: Build prefix sum matrix `pref` where `pref[i][j]` is sum of submatrix `(0,0)` to `(i,j)`.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Build prefix sum matrix `pref` where `pref[i][j]` is sum of submatrix `(0,0)` to `(i,j)`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Prefix Sum Matrix Build`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Submatrix Sum Query using Prefix Matrix

### What Exactly This Question Is Asking
- In simple words: Answer `Q` submatrix sum queries in O(1) each after preprocessing.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Answer `Q` submatrix sum queries in O(1) each after preprocessing.

### Input Format Interpretation
- `R C`
- matrix
- `Q`
- Q lines: `r1 c1 r2 c2` (0-based inclusive)

### Output Format Interpretation
- sum for each query

### Example 1 (Source-Style)
Input Format
- `R C`
- matrix
- `Q`
- Q lines: `r1 c1 r2 c2` (0-based inclusive)

### Output Format
- sum for each query

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int r, c;
    cin >> r >> c;

    vector<vector<long long>> pref(r + 1, vector<long long>(c + 1, 0));

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Submatrix Sum Query using Prefix Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Count Negatives in Sorted Matrix

### What Exactly This Question Is Asking
- In simple words: Rows and columns are non-increasing.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Rows and columns are non-increasing. Count negative numbers.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Count Negatives in Sorted Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Check Toeplitz Matrix

### What Exactly This Question Is Asking
- In simple words: A matrix is Toeplitz if all diagonals from top-left to bottom-right have same elements.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
A matrix is Toeplitz if all diagonals from top-left to bottom-right have same elements.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Check Toeplitz Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Lucky Numbers in Matrix

### What Exactly This Question Is Asking
- In simple words: A lucky number is minimum in its row and maximum in its column.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
A lucky number is minimum in its row and maximum in its column. Print all lucky numbers.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Lucky Numbers in Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Number of Islands (BFS)

### What Exactly This Question Is Asking
- In simple words: Count connected components of 1s in binary grid (4-direction).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Count connected components of 1s in binary grid (4-direction).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Number of Islands (BFS)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Flood Fill

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Flood Fill`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Rotting Oranges

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Rotting Oranges`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. 01 Matrix Nearest Zero Distance

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q27. 01 Matrix Nearest Zero Distance`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Count Distinct Paths in Grid (DP)

### What Exactly This Question Is Asking
- In simple words: Count ways to go from top-left to bottom-right with only right/down moves.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count ways to go from top-left to bottom-right with only right/down moves.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Count Distinct Paths in Grid (DP)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Unique Paths with Obstacles

### What Exactly This Question Is Asking
- In simple words: Grid has `0` free and `1` blocked.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Grid has `0` free and `1` blocked. Count paths top-left to bottom-right.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Unique Paths with Obstacles`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Maximum Path Sum from Top-left to Bottom-right (Right/Down)

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Maximum Path Sum from Top-left to Bottom-right (Right/Down)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Minimum Path Sum in Grid

### What Exactly This Question Is Asking
- In simple words: Use the title and provided context to infer required operation and return format.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Use the title and provided context to infer required operation and return format.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Minimum Path Sum in Grid`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. Sort Each Row of Matrix

### What Exactly This Question Is Asking
- In simple words: Sort each row independently in non-decreasing order.
- Core solving focus: apply correct ordering rule and preserve constraints/tie conditions.

### Problem Understanding (Detailed)
Sort each row independently in non-decreasing order.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q32. Sort Each Row of Matrix`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [9,1,7,3]`
Walkthrough: Walkthrough: Apply the exact sorting condition (ascending/custom comparator).
Expected Output: ordered sequence following question rule.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q33. Rotate Each Layer of Matrix by One (Clockwise)

### What Exactly This Question Is Asking
- In simple words: Rotate each ring/layer by one step clockwise.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Rotate each ring/layer by one step clockwise.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q33. Rotate Each Layer of Matrix by One (Clockwise)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q34. Story-Based Parking Lot Occupancy Grid

### What Exactly This Question Is Asking
- In simple words: Given `R x C` parking grid (`1` occupied, `0` free): 1.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Given `R x C` parking grid (`1` occupied, `0` free):
1. total occupied spots
2. row index with max occupancy (smallest index if tie)

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q34. Story-Based Parking Lot Occupancy Grid`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q35. Story-Based CCTV Visibility Grid

### What Exactly This Question Is Asking
- In simple words: Given grid of heights, a cell is visible if all cells before it in row from left are smaller.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Given grid of heights, a cell is visible if all cells before it in row from left are smaller.
Count visible cells row-wise.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q35. Story-Based CCTV Visibility Grid`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q36. Story-Based Robot Path in Grid Commands

### What Exactly This Question Is Asking
- In simple words: Given `R x C` grid with blocked cells and command string (R,L,U,D), start `(sr,sc)`.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Given `R x C` grid with blocked cells and command string (R,L,U,D), start `(sr,sc)`.
Stop command if next move is out of bounds or blocked. Print final position.

### Input Format Interpretation
- `R C`
- grid (`0` free, `1` blocked)
- `sr sc`
- command string

### Output Format Interpretation
- final row and col

### Example 1 (Source-Style)
Input Format
- `R C`
- grid (`0` free, `1` blocked)
- `sr sc`
- command string

### Output Format
- final row and col

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int r, c;
    cin >> r >> c;

    vector<vector<int>> g(r, vector<int>(c));
    for (int i = 0; i < r;

Detailed Explanation:
- Start by identifying required operation from title: `Q36. Story-Based Robot Path in Grid Commands`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q37. Story-Based Room Gold Collection (Max Contiguous Segment in Row)

### What Exactly This Question Is Asking
- In simple words: Each row has room gold values.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Each row has room gold values. For each row, find max contiguous segment sum and print global maximum.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q37. Story-Based Room Gold Collection (Max Contiguous Segment in Row)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q38. Story-Based Event Hall Seat Blocks (Largest Rectangle of 1s)

### What Exactly This Question Is Asking
- In simple words: Given binary matrix representing usable seats (`1`), find largest rectangular block of usable seats.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given binary matrix representing usable seats (`1`), find largest rectangular block of usable seats.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q38. Story-Based Event Hall Seat Blocks (Largest Rectangle of 1s)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q39. Story-Based Virus Spread Time in Lab Grid

### What Exactly This Question Is Asking
- In simple words: Grid has `0` empty, `1` healthy, `2` infected.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Grid has `0` empty, `1` healthy, `2` infected. Infection spreads 4-dir each minute.
Find time to infect all healthy else `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q39. Story-Based Virus Spread Time in Lab Grid`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q40. Story-Based Warehouse Distance to Exit (Multi-source BFS)

### What Exactly This Question Is Asking
- In simple words: Given grid where exits are marked `1`, compute each cell’s shortest distance to any exit (walls `-1`).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given grid where exits are marked `1`, compute each cell’s shortest distance to any exit (walls `-1`).

### Input Format Interpretation
- `R C`
- grid values (`1` exit, `0` empty, `-1` wall)

### Output Format Interpretation
- distance matrix (`-1` for unreachable/wall)

### Example 1 (Source-Style)
Input Format
- `R C`
- grid values (`1` exit, `0` empty, `-1` wall)

### Output Format
- distance matrix (`-1` for unreachable/wall)

### C++ Solution
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int r, c;
    cin >> r >> c;

    vector<vector<int>> g(r, vector<int>(c));

Detailed Explanation:
- Start by identifying required operation from title: `Q40. Story-Based Warehouse Distance to Exit (Multi-source BFS)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `matrix = [[5]]`
Walkthrough: Walkthrough: Single-cell matrix validates boundary checks and base case logic.
Expected Output: result for minimal matrix as per question requirement.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
