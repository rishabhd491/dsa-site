# TCS_NQT_DP_Master_CPP.md: Question Explaination (Each Question with 2 Examples)

This file explains what each question exactly asks and adds two detailed examples per question.

## Q1. Fibonacci Number (Tabulation)

### What Exactly This Question Is Asking
- In simple words: Given `n`, find `n-th` Fibonacci number (`F(0)=0, F(1)=1`).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given `n`, find `n-th` Fibonacci number (`F(0)=0, F(1)=1`).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q1. Fibonacci Number (Tabulation)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q2. Climbing Stairs

### What Exactly This Question Is Asking
- In simple words: You can climb `1` or `2` steps.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
You can climb `1` or `2` steps. Find number of ways to reach step `n`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q2. Climbing Stairs`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q3. Min Cost Climbing Stairs

### What Exactly This Question Is Asking
- In simple words: Given cost array where `cost[i]` is cost of step `i`, you can move 1 or 2 steps.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given cost array where `cost[i]` is cost of step `i`, you can move 1 or 2 steps.
Find minimum cost to reach top (after last index).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q3. Min Cost Climbing Stairs`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q4. Frog Jump (Min Energy)

### What Exactly This Question Is Asking
- In simple words: From stair `i`, frog jumps to `i+1` or `i+2`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
From stair `i`, frog jumps to `i+1` or `i+2`.
Energy cost between `i` and `j` is `abs(h[i]-h[j])`.
Find minimum energy to reach last stair.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q4. Frog Jump (Min Energy)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q5. House Robber

### What Exactly This Question Is Asking
- In simple words: You cannot rob adjacent houses.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
You cannot rob adjacent houses. Maximize stolen amount.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q5. House Robber`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q6. House Robber II (Circular Street)

### What Exactly This Question Is Asking
- In simple words: First and last houses are adjacent (circular array).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
First and last houses are adjacent (circular array). Max robbery amount?

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q6. House Robber II (Circular Street)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q7. Coin Change (Minimum Coins)

### What Exactly This Question Is Asking
- In simple words: Given coin denominations and target amount, find minimum coins needed.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given coin denominations and target amount, find minimum coins needed.
If not possible output `-1`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q7. Coin Change (Minimum Coins)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q8. Coin Change (Number of Ways)

### What Exactly This Question Is Asking
- In simple words: Count number of ways to form amount using unlimited coins.
- Core solving focus: define clear state transitions and base cases before iteration/recursion.

### Problem Understanding (Detailed)
Count number of ways to form amount using unlimited coins.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q8. Coin Change (Number of Ways)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q9. 0/1 Knapsack

### What Exactly This Question Is Asking
- In simple words: Given weights and values of `N` items, maximize value under capacity `W`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given weights and values of `N` items, maximize value under capacity `W`.
Each item can be chosen at most once.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q9. 0/1 Knapsack`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q10. Unbounded Knapsack

### What Exactly This Question Is Asking
- In simple words: Each item can be picked unlimited times.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Each item can be picked unlimited times. Maximize value under capacity `W`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q10. Unbounded Knapsack`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q11. Rod Cutting

### What Exactly This Question Is Asking
- In simple words: Given price of rods of length `1..N`, find max price for rod length `N`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given price of rods of length `1..N`, find max price for rod length `N`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q11. Rod Cutting`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q12. Subset Sum (Decision)

### What Exactly This Question Is Asking
- In simple words: Given array and target `S`, print `Yes` if subset with sum `S` exists else `No`.
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Given array and target `S`, print `Yes` if subset with sum `S` exists else `No`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q12. Subset Sum (Decision)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q13. Partition Equal Subset Sum

### What Exactly This Question Is Asking
- In simple words: Can array be divided into two subsets with equal sum?
- Core solving focus: set base case first, then recurse with valid reduced state.

### Problem Understanding (Detailed)
Can array be divided into two subsets with equal sum?

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q13. Partition Equal Subset Sum`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q14. Count Subsets with Sum K

### What Exactly This Question Is Asking
- In simple words: Count how many subsets have sum exactly `K`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Count how many subsets have sum exactly `K`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q14. Count Subsets with Sum K`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q15. Target Sum Ways (+/- Signs)

### What Exactly This Question Is Asking
- In simple words: Assign `+` or `-` to each number so expression equals target `T`.
- Core solving focus: define clear state transitions and base cases before iteration/recursion.

### Problem Understanding (Detailed)
Assign `+` or `-` to each number so expression equals target `T`.
Return count of ways.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q15. Target Sum Ways (+/- Signs)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q16. Longest Increasing Subsequence (LIS)

### What Exactly This Question Is Asking
- In simple words: Find length of longest strictly increasing subsequence.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find length of longest strictly increasing subsequence.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q16. Longest Increasing Subsequence (LIS)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q17. Maximum Sum Increasing Subsequence

### What Exactly This Question Is Asking
- In simple words: Find increasing subsequence with maximum sum.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Find increasing subsequence with maximum sum.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q17. Maximum Sum Increasing Subsequence`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q18. Longest Common Subsequence (LCS)

### What Exactly This Question Is Asking
- In simple words: Given strings `A` and `B`, find length of longest common subsequence.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Given strings `A` and `B`, find length of longest common subsequence.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q18. Longest Common Subsequence (LCS)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q19. Longest Common Substring

### What Exactly This Question Is Asking
- In simple words: Find length of longest common **contiguous** substring.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find length of longest common **contiguous** substring.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q19. Longest Common Substring`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q20. Edit Distance

### What Exactly This Question Is Asking
- In simple words: Minimum operations (insert/delete/replace) to convert string `A` into `B`.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Minimum operations (insert/delete/replace) to convert string `A` into `B`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q20. Edit Distance`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q21. Distinct Subsequences Count

### What Exactly This Question Is Asking
- In simple words: Given strings `S` and `T`, count subsequences of `S` equal to `T`.
- Core solving focus: build/update counts correctly and return requested frequency metric.

### Problem Understanding (Detailed)
Given strings `S` and `T`, count subsequences of `S` equal to `T`.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q21. Distinct Subsequences Count`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `arr = [4,4,2,1,4,2]`
Walkthrough: Walkthrough: Build counts and return the requested frequency-based result.
Expected Output: based on asked metric (e.g., max-frequency element/count).

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q22. Longest Palindromic Subsequence

### What Exactly This Question Is Asking
- In simple words: Find length of longest subsequence that is palindrome.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Find length of longest subsequence that is palindrome.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q22. Longest Palindromic Subsequence`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q23. Minimum Insertions to Make Palindrome

### What Exactly This Question Is Asking
- In simple words: Find minimum characters to insert in a string to make it palindrome.
- Core solving focus: identify symmetry logic and compare mirrored positions.

### Problem Understanding (Detailed)
Find minimum characters to insert in a string to make it palindrome.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q23. Minimum Insertions to Make Palindrome`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: `s = "abba"`
Walkthrough: Walkthrough: Compare first-last and inward pairs; all pairs match.
Expected Output: `Yes` / `true`.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q24. Unique Paths in Grid

### What Exactly This Question Is Asking
- In simple words: Robot moves only right/down in `m x n` grid.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Robot moves only right/down in `m x n` grid. Find number of paths.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q24. Unique Paths in Grid`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q25. Unique Paths with Obstacles

### What Exactly This Question Is Asking
- In simple words: Grid has blocked cells (`1` blocked, `0` free).
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
Grid has blocked cells (`1` blocked, `0` free). Count paths from top-left to bottom-right.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q25. Unique Paths with Obstacles`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q26. Minimum Path Sum

### What Exactly This Question Is Asking
- In simple words: Find minimum sum path from top-left to bottom-right (right/down only).
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Find minimum sum path from top-left to bottom-right (right/down only).

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q26. Minimum Path Sum`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q27. Triangle Minimum Path Sum

### What Exactly This Question Is Asking
- In simple words: Given triangle with `n` rows, move from row `i` index `j` to `i+1` index `j` or `j+1`.
- Core solving focus: prove why local optimal choice leads to global optimum.

### Problem Understanding (Detailed)
Given triangle with `n` rows, move from row `i` index `j` to `i+1` index `j` or `j+1`.
Find minimum total.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q27. Triangle Minimum Path Sum`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q28. Decode Ways

### What Exactly This Question Is Asking
- In simple words: String contains digits `1-26` mapped to `A-Z`.
- Core solving focus: define clear state transitions and base cases before iteration/recursion.

### Problem Understanding (Detailed)
String contains digits `1-26` mapped to `A-Z`. Count decode ways.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q28. Decode Ways`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q29. Matrix Chain Multiplication (Minimum Multiplications)

### What Exactly This Question Is Asking
- In simple words: Given dimensions array `p` of size `n`, matrix `i` has dimension `p[i-1] x p[i]`.
- Core solving focus: handle row/column traversal and boundary checks safely.

### Problem Understanding (Detailed)
Given dimensions array `p` of size `n`, matrix `i` has dimension `p[i-1] x p[i]`.
Find minimum scalar multiplications.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q29. Matrix Chain Multiplication (Minimum Multiplications)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q30. Maximum Subarray Sum (Kadane)

### What Exactly This Question Is Asking
- In simple words: Find contiguous subarray with maximum sum.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Find contiguous subarray with maximum sum.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q30. Maximum Subarray Sum (Kadane)`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q31. Maximum Product Subarray

### What Exactly This Question Is Asking
- In simple words: Find contiguous subarray with maximum product.
- Core solving focus: track a moving range and update answer when condition is valid.

### Problem Understanding (Detailed)
Find contiguous subarray with maximum product.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q31. Maximum Product Subarray`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.

---

## Q32. Stock Buy-Sell with Cooldown

### What Exactly This Question Is Asking
- In simple words: You may buy/sell stock many times, but after selling one day cooldown is required.
- Core solving focus: map story/rules to exact operations and apply them in strict order.

### Problem Understanding (Detailed)
You may buy/sell stock many times, but after selling one day cooldown is required.
Find maximum profit.

### Example 1 (Conceptual)
Input: Use a normal-size test case from mid-range constraints.
Walkthrough: Apply the rule in the question step-by-step and track state changes.
Expected Output: final value/structure requested by the question.

Detailed Explanation:
- Start by identifying required operation from title: `Q32. Stock Buy-Sell with Cooldown`.
- Parse input exactly in the expected format and map it to variables/containers.
- Apply algorithmic rule repeatedly until full input is processed.
- Return/print result in exact output format (single value / array / string / structure).

### Example 2 (Edge/Variant Case)
Input: smallest valid `n` (e.g., `n = 0` or `n = 1`)
Walkthrough: Walkthrough: Check base states before transitions to avoid wrong recurrence use.
Expected Output: base-case DP value.

Detailed Explanation:
- This second example stresses a boundary or tricky variant of the same question.
- Validate index bounds, empty/single-item behavior, and strict output formatting.
- Confirm final result remains correct even in minimal/extreme configurations.
